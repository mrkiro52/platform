import { TheoryCode, TheoryExample } from './components/TheoryTable'

function QA({ n, q, children }) {
  return (
    <div style={{ margin: '20px 0', paddingBottom: 18, borderBottom: '1px solid var(--border-color)' }}>
      <div style={{ display: 'flex', gap: 10, alignItems: 'flex-start', marginBottom: 8 }}>
        <span style={{
          flexShrink: 0, width: 24, height: 24, borderRadius: '50%', background: 'var(--accent-lime)',
          color: '#0a0a14', fontSize: 12, fontWeight: 800, display: 'flex', alignItems: 'center',
          justifyContent: 'center', marginTop: 2,
        }}>{n}</span>
        <p style={{ margin: 0, fontWeight: 700, color: 'var(--text-primary)', fontSize: 16 }}>{q}</p>
      </div>
      <div style={{ marginLeft: 34, color: 'var(--text-secondary)', fontSize: 14.5, lineHeight: 1.75 }}>
        {children}
      </div>
    </div>
  )
}

function Section({ title }) {
  return <h2 className="theory-heading-2">{title}</h2>
}

export default function July22FullstackInterviewTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Вопросы с собеседования: Фуллстак разработка</h1>
        <p className="theory-subtitle">Треки: Frontend и Backend</p>
        <p className="theory-date">22 июля 2026</p>
        <p>
          Тренировка на самых популярных вопросах для фуллстак-разработчиков. Первый блок — общие вопросы про
          веб-разработку и бэкенд (HTTP, API, базы данных, архитектура). Второй блок — вопросы по фронтенду и
          JavaScript/React. Формат — вопрос и развёрнутый ответ.
        </p>
      </section>

      <section className="theory-section">
        <Section title="Часть 1. Веб-разработка и бэкенд" />

        <QA n={1} q="Кто такой фуллстак-разработчик?">
          Разработчик, который может работать и с клиентской частью приложения (фронтенд — то, что видит и с чем
          взаимодействует пользователь в браузере), и с серверной частью (бэкенд — логика, база данных, API). Не
          обязательно быть экспертом в обеих областях одинаково глубоко — важно понимать, как части системы
          работают вместе.
        </QA>

        <QA n={2} q="В чём разница между фронтендом и бэкендом?">
          Фронтенд отвечает за интерфейс: вёрстку, стили, интерактивность, то, что происходит в браузере
          пользователя. Бэкенд отвечает за то, что происходит на сервере: бизнес-логику, работу с базой данных,
          авторизацию, API, которое отдаёт данные фронтенду.
        </QA>

        <QA n={3} q="Что такое REST API?">
          Архитектурный стиль построения API поверх HTTP, где каждый ресурс (пользователь, заказ, товар) доступен
          по своему URL, а действия над ним выражаются HTTP-методами: получить, создать, изменить, удалить. REST
          подразумевает отсутствие состояния на сервере между запросами (stateless).
        </QA>

        <QA n={4} q="Какие бывают основные HTTP-методы и для чего они?">
          <code>GET</code> — получить данные, не изменяя их. <code>POST</code> — создать новую запись.{' '}
          <code>PUT</code> — полностью заменить существующую запись. <code>PATCH</code> — частично обновить
          запись. <code>DELETE</code> — удалить запись.
          <TheoryCode language="text" code={`GET    /api/users/42     -> получить пользователя с id 42
POST   /api/users        -> создать нового пользователя
PUT    /api/users/42      -> полностью заменить данные пользователя 42
PATCH  /api/users/42      -> обновить только часть полей пользователя 42
DELETE /api/users/42      -> удалить пользователя 42`} />
        </QA>

        <QA n={5} q="Что означают основные группы кодов HTTP-статусов?">
          2xx — успех (200 OK, 201 Created). 3xx — перенаправление (301 Moved Permanently, 304 Not Modified). 4xx —
          ошибка на стороне клиента (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found). 5xx — ошибка
          на стороне сервера (500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable).
        </QA>

        <QA n={6} q="Что такое CORS и зачем он нужен?">
          Cross-Origin Resource Sharing — механизм браузерной безопасности, который по умолчанию запрещает
          странице с одного домена делать запросы к серверу на другом домене. Сервер явно разрешает нужные домены
          через заголовки ответа (например, <code>Access-Control-Allow-Origin</code>), чтобы фронтенд с одного
          адреса мог безопасно обращаться к API на другом.
        </QA>

        <QA n={7} q="Что такое JWT (JSON Web Token) и как он работает?">
          Компактный формат токена для передачи проверяемой информации (например, кто авторизован) между клиентом
          и сервером. Состоит из заголовка, полезной нагрузки (данные о пользователе) и подписи — сервер проверяет
          подпись, чтобы убедиться, что токен не подделан, не обращаясь при этом к базе данных на каждый запрос.
        </QA>

        <QA n={8} q="В чём разница между SQL и NoSQL базами данных?">
          SQL-базы (PostgreSQL, MySQL) хранят данные в строго структурированных таблицах со связями и гарантируют
          строгую целостность через транзакции. NoSQL-базы (MongoDB, Redis) хранят данные более гибко (документы,
          пары ключ-значение), проще масштабируются горизонтально, но обычно жертвуют частью строгих гарантий
          целостности ради скорости и гибкости.
        </QA>

        <QA n={9} q="Что такое ORM?">
          Object-Relational Mapping — библиотека, позволяющая работать с базой данных через объекты и методы языка
          программирования вместо написания «сырого» SQL вручную. Например, в Django ORM запрос{' '}
          <code>User.objects.filter(age__gt=18)</code> сам генерирует нужный SQL-запрос.
        </QA>

        <QA n={10} q="Зачем нужно кеширование и где его применяют?">
          Кеширование сохраняет результат «дорогой» операции (запрос к базе, тяжёлое вычисление), чтобы повторно
          не выполнять её заново, а отдать уже готовый ответ. Применяют на уровне HTTP (кеш браузера, CDN), на
          уровне приложения (Redis, Memcached) и на уровне базы данных.
        </QA>

        <QA n={11} q="Что такое CI/CD?">
          Continuous Integration / Continuous Deployment — практика автоматической проверки (тесты, линтеры) кода
          при каждом изменении (CI) и автоматической доставки готового кода в продакшен (CD), без ручных
          повторяющихся шагов разработчика.
        </QA>

        <QA n={12} q="В чём разница между монолитом и микросервисной архитектурой?">
          Монолит — всё приложение единая программа с общей базой данных, проще писать и деплоить на старте.
          Микросервисы — приложение разбито на независимые сервисы, каждый со своей зоной ответственности и часто
          своей базой данных, что даёт независимый деплой и масштабирование, но добавляет сложность распределённой
          системы.
        </QA>

        <QA n={13} q="Зачем нужен Docker в разработке?">
          Docker упаковывает приложение вместе со всем его окружением (версии языка, библиотеки, настройки) в
          переносимый контейнер, который запускается одинаково на любой машине — у разработчика, у коллеги, на
          сервере — устраняя проблему «работает только у меня».
        </QA>

        <QA n={14} q="Что такое горизонтальное и вертикальное масштабирование?">
          Вертикальное масштабирование — увеличение мощности одного сервера (больше CPU, памяти). Горизонтальное
          масштабирование — добавление новых серверов, между которыми распределяется нагрузка. Горизонтальное
          масштабирование обычно предпочтительнее для больших нагрузок, но требует, чтобы приложение умело работать
          в нескольких экземплярах одновременно (было stateless).
        </QA>

        <TheoryExample title="Совет по подготовке">
          На собеседовании фуллстак-разработчика часто просят нарисовать схему целиком — от запроса в браузере до
          ответа из базы данных. Потренируйся проговаривать этот путь: браузер → DNS → сервер → API → бизнес-логика
          → база данных → и обратно.
        </TheoryExample>
      </section>

      <section className="theory-section">
        <Section title="Часть 2. Фронтенд и JavaScript / React" />

        <QA n={15} q="Что такое Virtual DOM в React?">
          Облегчённая копия реального DOM в памяти. При изменении состояния React сначала обновляет Virtual DOM,
          сравнивает его с предыдущей версией (diffing) и вносит в реальный DOM только те изменения, которые
          реально нужны — это быстрее, чем перестраивать весь DOM заново при каждом изменении.
        </QA>

        <QA n={16} q="В чём разница между state и props в React?">
          <code>props</code> — данные, которые компонент получает извне, от родителя, и не может менять сам.{' '}
          <code>state</code> — внутреннее состояние компонента, которое он создаёт и меняет сам (например, через{' '}
          <code>useState</code>), и изменение которого вызывает перерисовку компонента.
        </QA>

        <QA n={17} q="Что такое хуки (hooks) в React?">
          Специальные функции, начинающиеся с <code>use</code>, которые позволяют функциональным компонентам
          «подключаться» к возможностям React — состоянию (<code>useState</code>), побочным эффектам (
          <code>useEffect</code>), контексту (<code>useContext</code>) — без необходимости писать классовые
          компоненты.
        </QA>

        <QA n={18} q="В чём разница между == и === в JavaScript?">
          <code>==</code> сравнивает значения с приведением типов (например, <code>"5" == 5</code> вернёт{' '}
          <code>true</code>, потому что строка сначала приводится к числу). <code>===</code> сравнивает значение и
          тип одновременно, без приведения (<code>"5" === 5</code> вернёт <code>false</code>). В современном коде
          почти всегда используют <code>===</code>, чтобы избежать неочевидного поведения.
        </QA>

        <QA n={19} q="Что такое замыкание (closure) в JavaScript?">
          Функция, которая «запоминает» переменные из окружения, в котором была создана, даже после того, как это
          окружение (например, внешняя функция) уже завершило выполнение.
          <TheoryCode language="javascript" code={`function makeCounter() {
  let count = 0
  return function () {
    count += 1
    return count
  }
}

const counter = makeCounter()
console.log(counter())  // 1
console.log(counter())  // 2 — count "запомнилась" между вызовами`} />
        </QA>

        <QA n={20} q="Что такое event loop в JavaScript?">
          Механизм, который позволяет однопоточному JavaScript обрабатывать асинхронные операции (сетевые запросы,
          таймеры), не блокируя основной поток: пока код ждёт результата асинхронной операции, event loop
          продолжает обрабатывать другие задачи, а обработчик результата выполнится, когда результат будет готов и
          основной поток освободится.
        </QA>

        <QA n={21} q="Что такое async/await и зачем это нужно?">
          Синтаксис поверх промисов, позволяющий писать асинхронный код, который выглядит и читается как обычный
          последовательный синхронный код, без цепочек <code>.then()</code>.
          <TheoryCode language="javascript" code={`async function getUser(id) {
  const response = await fetch(\`/api/users/\${id}\`)
  const user = await response.json()
  return user
}`} />
        </QA>

        <QA n={22} q="В чём разница между CSR и SSR?">
          CSR (Client-Side Rendering) — HTML почти пустой, страница собирается в браузере из JavaScript уже после
          загрузки. SSR (Server-Side Rendering) — HTML с готовым контентом формируется на сервере при каждом
          запросе и сразу отдаётся браузеру, что ускоряет первую отрисовку и улучшает SEO.
        </QA>

        <QA n={23} q="Что такое веб-сокеты (WebSocket) и чем они отличаются от обычных HTTP-запросов?">
          WebSocket устанавливает постоянное двустороннее соединение между клиентом и сервером, по которому оба
          могут отправлять сообщения в любой момент, без необходимости заново открывать соединение — в отличие от
          обычного HTTP-запроса, где клиент всегда сам инициирует запрос и получает один ответ.
        </QA>

        <QA n={24} q="Какие основные команды git нужно знать каждому разработчику?">
          <code>git clone</code> — скачать репозиторий, <code>git add</code> — добавить изменения в индекс,{' '}
          <code>git commit</code> — зафиксировать изменения, <code>git push</code> / <code>git pull</code> —
          отправить/забрать изменения с удалённого репозитория, <code>git branch</code> и <code>git checkout</code>{' '}
          — работа с ветками, <code>git merge</code> — слияние веток.
        </QA>
      </section>
    </div>
  )
}
