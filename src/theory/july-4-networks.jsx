import { TheoryTable, TheoryExample } from './components/TheoryTable'

export default function July4NetworksTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Компьютерные сети: основы</h1>
        <p className="theory-subtitle">Трек: Кибербезопасность</p>
        <p className="theory-date">4 июля 2026</p>
        <p>
          Без понимания того, как устройства обмениваются данными по сети, невозможно ни атаковать, ни защищать
          систему — большинство атак и защит работают именно на сетевом уровне.
        </p>
      </section>

      {/* Модель OSI/TCP-IP */}
      <section className="theory-section">
        <h2 className="theory-heading-2">1. Модели сети: OSI и TCP/IP</h2>
        <p>
          Передача данных по сети разбита на уровни — каждый решает свою задачу и не знает о деталях
          остальных. Полная модель OSI состоит из 7 уровней, но на практике чаще используют упрощённую
          модель TCP/IP из 4 уровней.
        </p>
        <TheoryTable
          headers={['Уровень TCP/IP', 'Соответствует OSI', 'За что отвечает', 'Примеры']}
          rows={[
            ['Прикладной', 'Приложение, представление, сеанс (5-7)', 'Формат данных для программ', 'HTTP, DNS, FTP, SMTP'],
            ['Транспортный', 'Транспортный (4)', 'Доставка данных между приложениями', 'TCP, UDP'],
            ['Сетевой (интернет)', 'Сетевой (3)', 'Маршрутизация между сетями', 'IP, ICMP'],
            ['Канальный/физический', 'Канальный, физический (1-2)', 'Передача битов по конкретной среде', 'Ethernet, Wi-Fi'],
          ]}
        />
        <TheoryExample title="Зачем это знать">
          Когда ты открываешь сайт, данные проходят вниз по уровням на твоём компьютере (упаковываются),
          передаются по проводам/эфиру, а на сервере поднимаются вверх по тем же уровням (распаковываются).
          Каждый уровень добавляет свой «заголовок» с служебной информацией.
        </TheoryExample>
      </section>

      {/* IP-адреса */}
      <section className="theory-section">
        <h2 className="theory-heading-2">2. IP-адреса</h2>
        <p>
          <strong>IP-адрес</strong> — уникальный номер устройства в сети, по которому к нему можно
          «достучаться» — как почтовый адрес дома.
        </p>
        <ul className="theory-list">
          <li><strong>IPv4</strong> — формат из 4 чисел 0-255 через точку: 192.168.1.10. Адресов ограниченно (≈4 млрд), поэтому их не хватает на все устройства мира.</li>
          <li><strong>IPv6</strong> — новый формат с намного большим пространством адресов (например, 2001:0db8::1), постепенно вытесняет IPv4.</li>
          <li><strong>Публичный IP</strong> — уникален во всём интернете, виден снаружи.</li>
          <li><strong>Приватный (локальный) IP</strong> — используется внутри локальной сети (диапазоны вида 192.168.x.x, 10.x.x.x), не виден напрямую из интернета.</li>
          <li><strong>NAT (Network Address Translation)</strong> — механизм, который позволяет множеству устройств с приватными IP выходить в интернет через один публичный IP роутера.</li>
        </ul>
      </section>

      {/* Порты */}
      <section className="theory-section">
        <h2 className="theory-heading-2">3. Порты</h2>
        <p>
          Если IP-адрес — это адрес дома, то <strong>порт</strong> — номер квартиры: конкретная служба на
          устройстве, к которой обращается запрос. Один сервер с одним IP может одновременно обслуживать сайт,
          почту и другие сервисы — каждый на своём порту.
        </p>
        <TheoryTable
          headers={['Порт', 'Служба']}
          rows={[
            ['80', 'HTTP (незашифрованный веб)'],
            ['443', 'HTTPS (защищённый веб)'],
            ['22', 'SSH (удалённое управление сервером)'],
            ['21', 'FTP (передача файлов)'],
            ['53', 'DNS'],
            ['25', 'SMTP (отправка почты)'],
          ]}
        />
        <TheoryExample title="Почему это важно в ИБ">
          Сканирование портов (port scanning) — первый шаг атакующего: он ищет, какие службы «слушают»
          на устройстве, чтобы найти уязвимую точку входа. Закрытие неиспользуемых портов файрволом —
          базовая защита.
        </TheoryExample>
      </section>

      {/* TCP vs UDP */}
      <section className="theory-section">
        <h2 className="theory-heading-2">4. TCP и UDP — два протокола транспортного уровня</h2>
        <TheoryTable
          headers={['Критерий', 'TCP', 'UDP']}
          rows={[
            ['Соединение', 'устанавливается заранее (handshake)', 'без установки соединения'],
            ['Надёжность', 'гарантирует доставку и порядок пакетов', 'не гарантирует ничего'],
            ['Скорость', 'медленнее из-за проверок', 'быстрее, минимум накладных расходов'],
            ['Применение', 'сайты, почта, файлы — важна целостность', 'видеозвонки, игры, стриминг — важна скорость'],
          ]}
        />
        <p>
          TCP перед передачей данных устанавливает соединение через <strong>three-way handshake</strong>
          («тройное рукопожатие»): клиент отправляет SYN → сервер отвечает SYN-ACK → клиент подтверждает ACK.
          После этого начинается обмен данными с гарантией доставки.
        </p>
      </section>

      {/* DNS */}
      <section className="theory-section">
        <h2 className="theory-heading-2">5. DNS — система доменных имён</h2>
        <p>
          Люди запоминают имена сайтов (google.com), а компьютеры общаются по IP-адресам. <strong>DNS</strong>
          — это «телефонная книга интернета», которая переводит доменное имя в IP-адрес.
        </p>
        <ul className="theory-list">
          <li>Ты вводишь в браузере google.com;</li>
          <li>компьютер спрашивает DNS-сервер: «какой IP у google.com?»;</li>
          <li>DNS-сервер отвечает конкретным IP-адресом;</li>
          <li>браузер уже обращается напрямую к этому IP.</li>
        </ul>
        <TheoryExample title="DNS-атаки">
          Если злоумышленник подменит ответ DNS (DNS spoofing), пользователь попадёт на поддельный сайт,
          думая, что зашёл на настоящий — классическая схема фишинга на уровне сети.
        </TheoryExample>
      </section>

      {/* HTTP/HTTPS */}
      <section className="theory-section">
        <h2 className="theory-heading-2">6. HTTP и HTTPS</h2>
        <p>
          <strong>HTTP</strong> (HyperText Transfer Protocol) — протокол прикладного уровня, по которому браузер
          и сервер обмениваются веб-страницами. Данные передаются в открытом виде — любой посередине пути
          (провайдер, публичный Wi-Fi) может их прочитать или подменить.
        </p>
        <p>
          <strong>HTTPS</strong> — тот же HTTP, но поверх <strong>TLS</strong> (Transport Layer Security):
          весь трафик шифруется, поэтому перехватить и прочитать данные (пароли, номера карт) нельзя.
        </p>
        <TheoryTable
          headers={['', 'HTTP', 'HTTPS']}
          rows={[
            ['Порт по умолчанию', '80', '443'],
            ['Шифрование', 'нет', 'да (TLS)'],
            ['Замок в браузере', 'нет / «Небезопасно»', 'есть'],
          ]}
        />
      </section>

      {/* Модель угроз в сети */}
      <section className="theory-section">
        <h2 className="theory-heading-2">7. Сетевые угрозы</h2>
        <TheoryTable
          headers={['Угроза', 'Суть']}
          rows={[
            ['MITM (Man-in-the-Middle)', 'злоумышленник встаёт «посередине» канала связи и читает/подменяет трафик'],
            ['DDoS', 'множество устройств одновременно засыпают сервер запросами, он перестаёт справляться'],
            ['Сканирование портов', 'поиск открытых/уязвимых служб на устройстве'],
            ['ARP-spoofing', 'подмена соответствия IP↔MAC в локальной сети для перехвата трафика'],
            ['DNS-spoofing', 'подмена ответа DNS, чтобы перенаправить жертву на поддельный ресурс'],
          ]}
        />
        <TheoryExample title="Базовая защита">
          HTTPS вместо HTTP защищает от чтения трафика при MITM. Файрвол ограничивает открытые порты. VPN
          шифрует весь трафик устройства, защищая его в публичных сетях (кафе, аэропорты).
        </TheoryExample>
      </section>

      {/* Выводы */}
      <section className="theory-section">
        <h2 className="theory-heading-2">8. Выводы</h2>
        <ul className="theory-list">
          <li>Сеть работает по уровням: прикладной → транспортный → сетевой → канальный; каждый уровень решает свою задачу.</li>
          <li>IP-адрес — «дом» устройства в сети, порт — «квартира» конкретной службы на нём.</li>
          <li>TCP — надёжно, но медленнее (веб, почта); UDP — быстро, но без гарантий (видео, игры).</li>
          <li>DNS переводит понятные людям имена в IP-адреса.</li>
          <li>HTTPS = HTTP + TLS-шифрование — обязателен для любого сайта, работающего с личными данными.</li>
          <li>Понимание сетевых угроз (MITM, DDoS, спуфинг) — основа для их предотвращения.</li>
        </ul>
      </section>
    </div>
  )
}
