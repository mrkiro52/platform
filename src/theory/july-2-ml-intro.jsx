import { TheoryTable, TheoryExample } from './components/TheoryTable'

function Formula({ children }) {
  return (
    <div style={{
      background: 'var(--bg-secondary)', border: '1px solid var(--border-color)', borderRadius: 8,
      padding: '14px 18px', margin: '14px 0', textAlign: 'center',
      fontFamily: '"Cambria Math", Georgia, "Times New Roman", serif', fontSize: 17,
      color: 'var(--text-primary)', overflowX: 'auto', lineHeight: 1.7,
    }}>{children}</div>
  )
}

export default function July2MlIntroTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Введение в машинное обучение</h1>
        <p className="theory-subtitle">Трек: Machine Learning</p>
        <p className="theory-date">2 июля 2026</p>
        <p>
          Машинное обучение — раздел искусственного интеллекта, изучающий методы построения алгоритмов,
          которые обучаются на данных: выявляют закономерности в примерах и применяют их к новым задачам
          без явного программирования правил.
        </p>
        <TheoryExample title="Классическое программирование vs ML">
          Классическое программирование: человек задаёт правила → данные → ответ. Машинное обучение:
          данные + размеченные ответы → алгоритм обучения → правила (модель), которая далее сама выдаёт
          ответы на новых данных.
        </TheoryExample>
      </section>

      {/* Определение Митчелла */}
      <section className="theory-section">
        <h2 className="theory-heading-2">1. Формальное определение Т. Митчелла</h2>
        <p>
          Программа обучается на опыте E относительно класса задач T и меры качества P, если её качество
          на задачах T, измеренное мерой P, улучшается с накоплением опыта E.
        </p>
        <TheoryTable
          headers={['Компонент', 'Значение']}
          rows={[
            ['T (Task)', 'Задача: класс задач, которые решает алгоритм (например, классификация писем)'],
            ['E (Experience)', 'Опыт: данные, на которых происходит обучение (обучающая выборка)'],
            ['P (Performance)', 'Мера качества: функционал, оценивающий, насколько хорошо модель решает задачу'],
          ]}
        />
      </section>

      {/* Объекты, признаки, выборка */}
      <section className="theory-section">
        <h2 className="theory-heading-2">2. Объекты, признаки, выборка</h2>
        <p><strong>Объекты и признаки:</strong></p>
        <ul className="theory-list">
          <li><strong>Объект (пример, sample)</strong> — единица данных, которую обрабатывает модель.</li>
          <li><strong>Признак (feature)</strong> — измеримое свойство объекта; объект описывается вектором признаков x ∈ X.</li>
          <li>Типы признаков: числовые, категориальные, порядковые, бинарные.</li>
          <li>Признаковое пространство X имеет размерность d.</li>
        </ul>
        <p><strong>Выборка и целевая переменная:</strong></p>
        <ul className="theory-list">
          <li><strong>Целевая переменная y ∈ Y</strong> — величина, которую модель должна предсказывать.</li>
          <li><strong>Обучающая выборка Dtrain</strong> — набор пар (xᵢ, yᵢ), на которых настраивается модель.</li>
          <li>Размеченная выборка — известны ответы yᵢ; неразмеченная — известны только объекты xᵢ.</li>
          <li>Наличие/отсутствие меток — основа деления на обучение с учителем и без учителя.</li>
        </ul>
      </section>

      {/* Модель и обучение */}
      <section className="theory-section">
        <h2 className="theory-heading-2">3. Модель, алгоритм обучения, функционал качества</h2>
        <ul className="theory-list">
          <li><strong>Модель (гипотеза)</strong> — функция a: X → Y, выбираемая из семейства A = {'{'}a(x; θ){'}'}, параметризованного вектором θ.</li>
          <li><strong>Алгоритм обучения</strong> — процедура, которая по обучающей выборке подбирает конкретную модель — находит θ*.</li>
          <li><strong>Параметры и гиперпараметры</strong> — параметры настраиваются автоматически (веса); гиперпараметры задаются заранее (сложность модели, скорость обучения).</li>
          <li><strong>Функция потерь ℓ(y, ŷ)</strong> — оценивает рассогласование истинного ответа и предсказания модели.</li>
        </ul>
        <p>Эмпирический риск и принцип ERM (обучение — минимизация Q(a) по параметрам модели):</p>
        <Formula>Q(a) = (1/n) Σ ℓ(yᵢ, a(xᵢ))</Formula>
      </section>

      {/* Обобщающая способность */}
      <section className="theory-section">
        <h2 className="theory-heading-2">4. Недообучение и переобучение</h2>
        <p>Цель обучения — не запомнить обучающую выборку, а хорошо работать на новых данных (обобщающая способность).</p>
        <TheoryTable
          headers={['Явление', 'Суть']}
          rows={[
            ['Недообучение (underfitting)', 'Модель слишком проста: высокая ошибка и на обучающей, и на новой выборке'],
            ['Переобучение (overfitting)', 'Модель слишком сложна и подстроилась под шум обучающих данных: ошибка на обучении мала, на новых данных — велика'],
          ]}
        />
        <ul className="theory-list">
          <li><strong>Дилемма смещения–разброса</strong> — простые модели дают большое смещение (bias), сложные — большой разброс (variance).</li>
          <li><strong>Контроль качества</strong> — оценка модели на отложенных данных: валидация и кросс-валидация.</li>
        </ul>
      </section>

      {/* Классификация задач */}
      <section className="theory-section">
        <h2 className="theory-heading-2">5. Четыре парадигмы машинного обучения</h2>
        <p>Основной критерий классификации — наличие «учителя», то есть присутствие целевой переменной в данных.</p>
        <ul className="theory-list">
          <li><strong>Обучение с учителем (supervised)</strong> — есть размеченные пары (xᵢ, yᵢ); цель — восстановить зависимость y = a(x).</li>
          <li><strong>Обучение без учителя (unsupervised)</strong> — есть только объекты xᵢ без ответов; цель — выявить внутреннюю структуру данных.</li>
          <li><strong>Частичное привлечение учителя (semi-supervised)</strong> — размечена малая часть выборки, используется вместе с неразмеченными данными.</li>
          <li><strong>Обучение с подкреплением (reinforcement)</strong> — агент получает сигнал вознаграждения от среды и вырабатывает стратегию действий.</li>
        </ul>
      </section>

      {/* С учителем */}
      <section className="theory-section">
        <h2 className="theory-heading-2">6. Обучение с учителем: постановка задачи</h2>
        <p>
          Дана размеченная выборка Dtrain = {'{'}(xᵢ, yᵢ){'}'}. Требуется построить модель a: X → Y,
          максимально близкую к неизвестной зависимости y*.
        </p>
        <ul className="theory-list">
          <li><strong>Классификация</strong> — множество ответов Y дискретно, Y = {'{'}c₁, ..., cₖ{'}'}; предсказывается метка класса (например, «спам / не спам»).</li>
          <li><strong>Регрессия</strong> — множество ответов Y непрерывно, Y = ℝ; предсказывается числовая величина (например, цена).</li>
        </ul>
        <p>Схема процесса: размеченные данные → алгоритм обучения → обученная модель a(x) → прогноз ŷ для нового объекта.</p>
        <TheoryExample title="Примеры задач">
          Фильтрация спама, кредитный скоринг, медицинская диагностика, прогноз стоимости недвижимости.
        </TheoryExample>
      </section>

      {/* Без учителя */}
      <section className="theory-section">
        <h2 className="theory-heading-2">7. Обучение без учителя: основные типы задач</h2>
        <p>Дана неразмеченная выборка D = {'{'}xᵢ{'}'}. Цель — обнаружить скрытую структуру данных без эталонных ответов.</p>
        <ul className="theory-list">
          <li><strong>Кластеризация</strong> — разбиение объектов на группы так, чтобы внутри группы объекты были похожи, а между группами — различны.</li>
          <li><strong>Снижение размерности</strong> — построение компактного представления данных с сохранением существенной информации.</li>
          <li><strong>Поиск ассоциативных правил</strong> — выявление устойчивых совместных зависимостей между признаками (анализ покупательских корзин).</li>
          <li><strong>Обнаружение аномалий</strong> — выделение объектов, резко отличающихся от основной массы данных.</li>
        </ul>
        <TheoryExample title="Примеры задач">
          Сегментация клиентов, группировка новостей по темам, выявление мошеннических операций.
        </TheoryExample>
      </section>

      {/* Сравнение */}
      <section className="theory-section">
        <h2 className="theory-heading-2">8. Обучение с учителем и без учителя</h2>
        <TheoryTable
          headers={['Критерий', 'С учителем', 'Без учителя']}
          rows={[
            ['Данные', 'Размеченные пары (xᵢ, yᵢ)', 'Только объекты xᵢ, без ответов'],
            ['Цель', 'Восстановить зависимость y = a(x)', 'Выявить скрытую структуру данных'],
            ['Наличие эталона', 'Есть правильные ответы', 'Эталонных ответов нет'],
            ['Типы задач', 'Классификация, регрессия', 'Кластеризация, снижение размерности, поиск аномалий'],
            ['Оценка качества', 'Прямая (сравнение с yᵢ)', 'Косвенная / экспертная интерпретация'],
            ['Стоимость данных', 'Высокая (нужна разметка)', 'Ниже (разметка не требуется)'],
          ]}
        />
      </section>

      {/* Выводы */}
      <section className="theory-section">
        <h2 className="theory-heading-2">9. Выводы</h2>
        <ul className="theory-list">
          <li>Машинное обучение — раздел ИИ, где правила решения задачи извлекаются из данных, а не программируются вручную; формально задаётся тройкой «задача T — опыт E — качество P».</li>
          <li>Ключевые понятия: объект и признаки, целевая переменная, обучающая выборка, модель и семейство гипотез, функция потерь и эмпирический риск, обобщающая способность.</li>
          <li>Обучение — это минимизация функционала качества (принцип ERM); контроль переобучения и недообучения обеспечивает обобщающую способность модели.</li>
          <li>Задачи ML делят прежде всего по наличию «учителя»: с учителем, без учителя, с частичным привлечением учителя и с подкреплением.</li>
          <li>В обучении с учителем задача делится на классификацию (дискретный ответ) и регрессию (числовой ответ).</li>
          <li>В обучении без учителя решаются задачи кластеризации, снижения размерности, поиска ассоциаций и аномалий — без эталонных ответов.</li>
        </ul>
      </section>
    </div>
  )
}
