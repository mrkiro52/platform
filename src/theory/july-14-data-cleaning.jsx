import { TheoryTable, TheoryCode, TheoryExample } from './components/TheoryTable'

function Fig({ children, caption }) {
  return (
    <figure style={{ margin: '18px 0', display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
      <div style={{
        width: '100%', maxWidth: 640, background: '#12121e', border: '1px solid #2a2a3a',
        borderRadius: 10, padding: '16px', display: 'flex', justifyContent: 'center', overflowX: 'auto',
      }}>{children}</div>
      {caption && <figcaption style={{ color: 'var(--text-tertiary)', fontSize: 12.5, textAlign: 'center', maxWidth: 640 }}>{caption}</figcaption>}
    </figure>
  )
}

function Term({ name, children }) {
  return (
    <div style={{ margin: '12px 0', paddingLeft: 14, borderLeft: '2px solid var(--accent-lime)' }}>
      <span style={{ color: 'var(--accent-lime)', fontWeight: 700 }}>{name}</span>
      <span style={{ color: 'var(--text-secondary)', fontSize: 14, lineHeight: 1.75 }}> — {children}</span>
    </div>
  )
}

function P({ n, children }) {
  return (
    <div style={{ display: 'flex', gap: 12, margin: '14px 0', alignItems: 'flex-start' }}>
      <span style={{
        flexShrink: 0, width: 26, height: 26, borderRadius: '50%', border: '1.5px solid var(--accent-lime)',
        color: 'var(--accent-lime)', fontSize: 12, fontWeight: 700, display: 'flex', alignItems: 'center',
        justifyContent: 'center', marginTop: 2,
      }}>{n}</span>
      <p style={{ margin: 0, flex: 1 }}>{children}</p>
    </div>
  )
}

export default function July14DataCleaningTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Очистка данных для аналитика</h1>
        <p className="theory-subtitle">Трек: Аналитика</p>
        <p className="theory-date">14 июля 2026</p>
        <p>
          Прежде чем строить графики, считать метрики и обучать модели, данные нужно <strong>очистить</strong>. По
          опыту, на очистку и подготовку данных уходит до 80% времени аналитика — «грязные» данные приводят к
          неверным выводам, каким бы точным ни был анализ дальше. Сегодня разберём максимально подробно: что вообще
          считать «грязью», какие бывают проблемы и как их чинить — и в Excel, и в Python (pandas).
        </p>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">1. Что такое очистка данных и зачем она нужна</h2>
        <Term name="Очистка данных (data cleaning)">
          процесс обнаружения и исправления (или удаления) ошибок, пропусков и несоответствий в наборе данных,
          чтобы привести его к пригодному для анализа виду.
        </Term>
        <P n={1}>
          Главный принцип аналитики — <strong>«мусор на входе — мусор на выходе»</strong> (garbage in, garbage
          out). Если в данных дубли, опечатки, пропуски и разные форматы, то любые расчёты по ним будут искажены:
          средний чек посчитается неверно, график покажет ложный тренд, а модель обучится на ошибках. Поэтому
          очистка — не рутина «на потом», а фундамент всего анализа.
        </P>
        <P n={2}>
          Очистку удобно вести по чек-листу: сначала <strong>осмотреть</strong> данные (размер, типы, примеры,
          пропуски), затем последовательно устранять проблемы — пропуски, дубликаты, лишние пробелы, неправильные
          типы, выбросы, несогласованные форматы — и в конце ещё раз проверить результат.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">2. Какие данные вообще стоит очищать (виды «грязи»)</h2>
        <TheoryTable
          headers={['Проблема', 'Пример']}
          rows={[
            ['Пропущенные значения', 'пустая ячейка возраста, NaN, "", "—", "н/д"'],
            ['Дубликаты', 'один и тот же клиент записан дважды'],
            ['Лишние пробелы', '" Москва ", "Иван\\t", двойные пробелы внутри'],
            ['Неправильные типы', 'число хранится как текст "1 000", дата как строка'],
            ['Несогласованные форматы', '"Москва" / "москва" / "МСК", даты ДД.ММ.ГГГГ vs ГГГГ-ММ-ДД'],
            ['Выбросы (аномалии)', 'возраст 200 лет, цена -50 ₽'],
            ['Опечатки и ошибки ввода', '"Малако", телефон из 5 цифр'],
            ['Неверные единицы', 'рост в метрах и в сантиметрах в одном столбце'],
          ]}
        />
        <P n={3}>
          Не всякая «необычность» — ошибка: прежде чем что-то менять, важно понять природу данных. Пропуск может
          означать «нет данных», а может — «значение равно нулю»; выброс может быть опечаткой, а может — реальным,
          но редким случаем. Поэтому очистка всегда идёт рука об руку с пониманием предметной области.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">3. Первый шаг — осмотр данных</h2>
        <P n={4}>
          Перед любыми правками данные нужно «прощупать»: сколько строк и столбцов, какие типы, где пропуски,
          как выглядят типичные значения. В Excel это визуальный просмотр, автофильтр и сводные таблицы; в pandas —
          несколько стандартных команд.
        </P>
        <TheoryCode language="python" code={`import pandas as pd

df = pd.read_csv('data.csv')

df.shape          # (число строк, число столбцов)
df.head()         # первые 5 строк — глазами оценить данные
df.info()         # типы столбцов и число непустых значений
df.describe()     # статистика по числовым столбцам (min, max, среднее)
df.isna().sum()   # сколько пропусков в каждом столбце
df.nunique()      # сколько уникальных значений в каждом столбце
df['city'].value_counts()   # частоты значений — видно опечатки и разнобой`} />
        <TheoryExample title="Зачем value_counts()">
          Метод <code>value_counts()</code> моментально показывает разнобой: если в столбце «город» встречаются
          «Москва», «москва», «МСК» и « Москва », они окажутся разными строками — это сразу видно и подсказывает,
          что нужно привести к единому виду.
        </TheoryExample>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">4. Пропущенные значения (missing values)</h2>
        <P n={5}>
          Сначала пропуски нужно <strong>найти</strong>, затем решить, что с ними делать. Есть три базовые
          стратегии: <strong>удалить</strong> строки/столбцы с пропусками, <strong>заполнить</strong> (imputation)
          осмысленным значением, либо <strong>оставить</strong> как отдельную категорию «неизвестно».
        </P>
        <TheoryTable
          headers={['Стратегия', 'Когда применять']}
          rows={[
            ['Удалить строки', 'пропусков мало, они случайны, данных достаточно'],
            ['Удалить столбец', 'в столбце пропущено большинство значений'],
            ['Заполнить средним/медианой', 'числовой признак, важно сохранить строки'],
            ['Заполнить модой', 'категориальный признак (самое частое значение)'],
            ['Заполнить «неизвестно»', 'пропуск сам по себе несёт смысл'],
          ]}
        />
        <TheoryCode language="python" code={`# найти пропуски
df.isna().sum()

# удалить строки, где есть пропуски
df.dropna()

# удалить столбец
df.drop(columns=['comment'])

# заполнить пропуски
df['age'] = df['age'].fillna(df['age'].median())   # медианой
df['city'] = df['city'].fillna('неизвестно')       # категорией

# заранее заменить "мусорные" обозначения пропуска на настоящий NaN
df = df.replace(['', '—', 'н/д', 'NULL'], pd.NA)`} />
        <P n={6}>
          <strong>В Excel:</strong> пропуски находят через фильтр (галочка «Пустые»), условное форматирование
          (подсветка пустых) или функцию <code>СЧИТАТЬПУСТОТЫ()</code>. Заполняют вручную, протягиванием или
          формулой <code>ЕСЛИ(ЕПУСТО(A2); среднее; A2)</code>. Медиану/среднее берут из <code>МЕДИАНА()</code> /{' '}
          <code>СРЗНАЧ()</code>.
        </P>
        <TheoryExample title="Осторожно со средним">
          Заполнять пропуски средним удобно, но опасно при выбросах: одна ошибочная цена в миллион завысит среднее.
          Часто безопаснее <strong>медиана</strong> — она устойчивее к аномалиям. А заполнение большого числа
          пропусков одним значением искусственно занижает разброс данных — это стоит учитывать.
        </TheoryExample>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">5. Дубликаты</h2>
        <Term name="Дубликат">
          полностью или частично повторяющаяся запись, которая искажает подсчёты (например, один заказ, посчитанный
          дважды, завысит выручку).
        </Term>
        <TheoryCode language="python" code={`df.duplicated().sum()        # сколько полных дубликатов строк

df = df.drop_duplicates()    # удалить полные дубликаты

# удалить дубликаты по ключевому столбцу (например, по email),
# оставив первую встреченную запись
df = df.drop_duplicates(subset=['email'], keep='first')`} />
        <P n={7}>
          <strong>В Excel:</strong> на вкладке «Данные» есть кнопка <strong>«Удалить дубликаты»</strong> — можно
          выбрать, по каким столбцам считать строки одинаковыми. Найти дубликаты, не удаляя, помогает условное
          форматирование «Повторяющиеся значения» или сводная таблица с подсчётом количества.
        </P>
        <TheoryExample title="Полные и частичные дубликаты">
          Полный дубликат — строки совпадают целиком. Частичный — совпадает ключ (email, номер заказа), но
          отличаются другие поля (например, разный статус). Частичные дубликаты опаснее: нужно решить, какую запись
          считать верной (обычно последнюю по времени).
        </TheoryExample>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">6. Лишние пробелы и trim</h2>
        <Term name="Trim">
          удаление пробелов (и других невидимых символов) в начале и конце строки. « Москва » и «Москва» для
          компьютера — разные значения, поэтому такие «хвосты» обязательно убирают.
        </Term>
        <P n={8}>
          Проблема коварна тем, что пробелы не видны глазом: два одинаковых на вид города не сгруппируются вместе,
          соединение таблиц по ключу «сломается», а <code>value_counts()</code> покажет лишние категории. Помимо
          крайних пробелов, чистят <strong>двойные пробелы внутри</strong> строки и невидимые символы (табуляция,
          перенос строки).
        </P>
        <TheoryCode language="python" code={`# убрать пробелы по краям
df['city'] = df['city'].str.strip()

# схлопнуть множественные пробелы внутри в один
df['name'] = df['name'].str.replace(r'\\s+', ' ', regex=True).str.strip()

# частый приём заодно с trim — привести к единому регистру
df['city'] = df['city'].str.strip().str.lower()`} />
        <P n={9}>
          <strong>В Excel:</strong> функция <code>СЖПРОБЕЛЫ()</code> (англ. <code>TRIM</code>) убирает лишние
          пробелы, оставляя одиночные между словами. Для невидимых непечатаемых символов есть <code>ПЕЧСИМВ()</code>{' '}
          (<code>CLEAN</code>). Регистр приводят функциями <code>СТРОЧН()</code>, <code>ПРОПИСН()</code>,{' '}
          <code>ПРОПНАЧ()</code>.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">7. Обработка типов данных</h2>
        <P n={10}>
          Каждый столбец должен иметь <strong>правильный тип</strong>: числа — числовыми, даты — датами, категории —
          текстом. Частая беда — числа, «приехавшие» как текст (из-за пробелов-разделителей тысяч, запятой вместо
          точки, значка валюты). По такому «числу» не посчитать сумму и не построить график, пока не преобразуешь
          тип.
        </P>
        <TheoryCode language="python" code={`df.dtypes    # посмотреть текущие типы столбцов

# текст "1 000,50 ₽" -> число 1000.50
df['price'] = (df['price']
    .str.replace(' ', '', regex=False)     # убрать разделитель тысяч
    .str.replace('₽', '', regex=False)     # убрать валюту
    .str.replace(',', '.', regex=False))   # запятая -> точка
df['price'] = pd.to_numeric(df['price'], errors='coerce')  # -> float, брак в NaN

# строка -> дата
df['date'] = pd.to_datetime(df['date'], errors='coerce', dayfirst=True)

# приведение типа
df['count'] = df['count'].astype('int')`} />
        <P n={11}>
          <strong>В Excel:</strong> тип виден по выравниванию (числа — справа, текст — слева) и по «зелёному
          уголку» ошибки. Текст в число превращают через <code>ЗНАЧЕН()</code> (<code>VALUE</code>) или «Данные →
          Текст по столбцам»; даты распознают тем же «Текст по столбцам» с указанием формата. Параметр{' '}
          <code>errors='coerce'</code> в pandas важен: он превращает «непереводимые» значения в NaN, а не роняет
          весь код с ошибкой.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">8. Приведение к единому формату (стандартизация)</h2>
        <P n={12}>
          Одни и те же по смыслу значения часто записаны по-разному. Их приводят к <strong>единому виду</strong>,
          иначе они будут считаться разными категориями:
        </P>
        <TheoryTable
          headers={['Что стандартизируют', 'Как']}
          rows={[
            ['Регистр текста', '«Москва»/«москва»/«МОСКВА» → один регистр'],
            ['Категории-синонимы', '«М»/«муж»/«мужской» → «мужской»'],
            ['Даты', 'единый формат ГГГГ-ММ-ДД'],
            ['Телефоны', 'единый вид +7XXXXXXXXXX, убрать скобки и дефисы'],
            ['Числа', 'единый разделитель дробной части и единицы измерения'],
          ]}
        />
        <TheoryCode language="python" code={`# сопоставление синонимов одному значению
mapping = {'м': 'мужской', 'муж': 'мужской', 'ж': 'женский', 'жен': 'женский'}
df['gender'] = df['gender'].str.strip().str.lower().map(mapping)

# телефон -> только цифры
df['phone'] = df['phone'].str.replace(r'\\D', '', regex=True)`} />
        <P n={13}>
          <strong>В Excel</strong> синонимы приводят через <code>ЕСЛИ()</code>/<code>ПЕРЕКЛЮЧ()</code> или
          справочник + <code>ВПР()</code> (<code>VLOOKUP</code>); даты — форматированием ячеек и «Текст по
          столбцам»; лишние символы убирают <code>ПОДСТАВИТЬ()</code> (<code>SUBSTITUTE</code>).
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">9. Выбросы и аномалии</h2>
        <Term name="Выброс (outlier)">
          значение, сильно выбивающееся из общего ряда: возраст 200 лет, отрицательная цена, чек в миллион среди
          сотен обычных. Может быть ошибкой ввода — или редким, но настоящим значением.
        </Term>
        <P n={14}>
          Выбросы находят по здравому смыслу (возраст не бывает отрицательным), по границам (<code>min</code>/
          <code>max</code> в <code>describe()</code>) и статистически — например, <strong>правилом
          межквартильного размаха (IQR)</strong>: всё, что за пределами [Q1 − 1.5·IQR; Q3 + 1.5·IQR], считается
          подозрительным. Дальше решают: исправить (если это опечатка), удалить или оставить (если значение
          реальное).
        </P>
        <TheoryCode language="python" code={`# границы по правилу IQR
q1 = df['price'].quantile(0.25)
q3 = df['price'].quantile(0.75)
iqr = q3 - q1
low, high = q1 - 1.5 * iqr, q3 + 1.5 * iqr

outliers = df[(df['price'] < low) | (df['price'] > high)]  # подозрительные

# явно невозможные значения
df = df[(df['age'] >= 0) & (df['age'] <= 120)]`} />
        <P n={15}>
          <strong>В Excel</strong> выбросы ищут сортировкой (крайние значения всплывают сверху/снизу), функциями{' '}
          <code>МИН()</code>/<code>МАКС()</code>/<code>КВАРТИЛЬ()</code> и диаграммой «ящик с усами» (boxplot),
          которая наглядно показывает аномальные точки.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">10. Excel или Python: что выбрать</h2>
        <TheoryTable
          headers={['Критерий', 'Excel', 'Python (pandas)']}
          rows={[
            ['Объём данных', 'до ~1 млн строк', 'миллионы+ строк'],
            ['Наглядность', 'видно каждую ячейку', 'работа «вслепую», через команды'],
            ['Повторяемость', 'делается заново руками', 'скрипт запускается повторно'],
            ['Автоматизация', 'ограничена', 'полная (можно по расписанию)'],
            ['Порог входа', 'низкий', 'нужно знать pandas'],
          ]}
        />
        <P n={16}>
          Практический вывод: <strong>Excel</strong> хорош для быстрого разового осмотра и небольших таблиц;{' '}
          <strong>Python (pandas)</strong> — когда данных много, очистку нужно повторять регулярно или встроить в
          пайплайн. Часто их сочетают: посмотрели в Excel — почистили скриптом.
        </P>
      </section>

      <section className="theory-section">
        <h2 className="theory-heading-2">Итоги</h2>
        <P n={17}>
          Очистка данных — фундамент анализа: «мусор на входе — мусор на выходе». Порядок работы: осмотреть данные
          → устранить <strong>пропуски</strong> (удалить/заполнить/оставить) → убрать <strong>дубликаты</strong> →
          сделать <strong>trim</strong> и почистить пробелы → привести <strong>типы</strong> (числа, даты) →{' '}
          <strong>стандартизировать</strong> форматы и категории → разобраться с <strong>выбросами</strong> →
          перепроверить. Мелкие таблицы удобно чистить в <strong>Excel</strong> (СЖПРОБЕЛЫ, Удалить дубликаты,
          Текст по столбцам), большие и повторяющиеся задачи — в <strong>Python/pandas</strong> (dropna, fillna,
          drop_duplicates, str.strip, to_numeric, to_datetime). Чистые данные — это уже половина хорошего анализа.
        </P>
      </section>
    </div>
  )
}
