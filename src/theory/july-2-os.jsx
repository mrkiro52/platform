import { TheoryTable, TheoryExample } from './components/TheoryTable'

/* Небольшая обёртка для SVG-иллюстрации с подписью */
function Fig({ children, caption }) {
  return (
    <figure style={{ margin: '18px 0', display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
      <div style={{
        width: '100%', maxWidth: 620, background: '#12121e', border: '1px solid #2a2a3a',
        borderRadius: 10, padding: '16px', display: 'flex', justifyContent: 'center', overflowX: 'auto',
      }}>{children}</div>
      {caption && <figcaption style={{ color: 'var(--text-tertiary)', fontSize: 12.5, textAlign: 'center' }}>{caption}</figcaption>}
    </figure>
  )
}

/* Определение термина: жирный термин + пояснение */
function Term({ name, children }) {
  return (
    <div style={{ margin: '12px 0', paddingLeft: 14, borderLeft: '2px solid var(--accent-lime)' }}>
      <span style={{ color: 'var(--accent-lime)', fontWeight: 700 }}>{name}</span>
      <span style={{ color: 'var(--text-secondary)', fontSize: 14, lineHeight: 1.75 }}> — {children}</span>
    </div>
  )
}

const C = { text: 'var(--text-primary)', sub: 'var(--text-secondary)', lime: '#FFD60A', box: '#1a1a24', border: '#2a2a3a' }

export default function July2OsTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Операционные системы: основы</h1>
        <p className="theory-subtitle">Трек: Кибербезопасность</p>
        <p className="theory-date">2 июля 2026</p>
        <p>
          Операционная система (ОС) — комплекс программ, который управляет всеми аппаратными ресурсами
          компьютера (процессором, памятью, диском, сетью, устройствами) и предоставляет приложениям единую
          удобную среду для работы. Без ОС каждая программа была бы вынуждена сама «общаться» с железом на
          языке нулей и единиц.
        </p>
        <p>
          Для специалиста по безопасности ОС — это фундамент. Именно на её уровне решается, какой процесс
          получит доступ к памяти, файлам и сети, кто может выполнять привилегированные операции, и как
          изолированы приложения друг от друга. Почти любая атака либо использует слабость ОС, либо
          защищается механизмами ОС.
        </p>
      </section>

      {/* Что такое ОС — слои */}
      <section className="theory-section">
        <h2 className="theory-heading-2">1. Где находится ОС: модель слоёв</h2>
        <p>
          Компьютер удобно представлять как «слоёный пирог». Пользователь и приложения не обращаются к железу
          напрямую — между ними стоит операционная система, а точнее её центральная часть — ядро.
        </p>
        <Fig caption="Каждый верхний слой обращается только к соседнему нижнему через строго определённый интерфейс">
          <svg viewBox="0 0 560 250" width="100%" style={{ maxWidth: 560 }} xmlns="http://www.w3.org/2000/svg">
            {[
              { y: 10,  label: 'Пользователь', sub: 'человек, клики, команды', fill: '#23202e' },
              { y: 58,  label: 'Приложения', sub: 'браузер, редактор, игры', fill: '#242b3a' },
              { y: 106, label: 'Операционная система (ядро + службы)', sub: 'управление ресурсами, безопасность', fill: 'rgba(255,214,10,0.12)', stroke: C.lime },
              { y: 154, label: 'Драйверы', sub: 'посредники между ОС и устройствами', fill: '#242b3a' },
              { y: 202, label: 'Аппаратное обеспечение (железо)', sub: 'CPU, RAM, диск, сеть', fill: '#23202e' },
            ].map((r, i) => (
              <g key={i}>
                <rect x="30" y={r.y} width="500" height="40" rx="7" fill={r.fill} stroke={r.stroke || C.border} />
                <text x="45" y={r.y + 18} fill={C.text} fontSize="13" fontWeight="700">{r.label}</text>
                <text x="45" y={r.y + 33} fill={C.sub} fontSize="11">{r.sub}</text>
              </g>
            ))}
          </svg>
        </Fig>
        <p>
          Чем ниже слой, тем больше у него власти над машиной. Поэтому доступ к нижним слоям строго ограничен:
          обычное приложение не может напрямую записать данные на диск или прочитать чужую память — оно обязано
          попросить об этом ядро. Это и есть основа безопасности.
        </p>
      </section>

      {/* Функции ОС */}
      <section className="theory-section">
        <h2 className="theory-heading-2">2. Основные функции ОС</h2>
        <Term name="Управление процессами">
          процесс — это запущенная программа со своей памятью и состоянием. ОС создаёт процессы, распределяет
          между ними процессорное время (планировщик), приостанавливает и завершает их. С точки зрения
          безопасности важно, что процессы изолированы: один не может «залезть» в память другого.
        </Term>
        <Term name="Управление памятью">
          ОС выделяет каждому процессу его собственную область оперативной памяти и защищает её от других
          процессов. Механизм виртуальной памяти делает так, что каждый процесс «думает», что владеет всей
          памятью, хотя физически она поделена. Нарушение границ памяти — источник многих уязвимостей
          (переполнение буфера).
        </Term>
        <Term name="Управление файлами">
          организация данных на диске в виде файлов и каталогов, а также хранение прав доступа: кто может
          читать, изменять и выполнять каждый файл.
        </Term>
        <Term name="Управление устройствами">
          работа с клавиатурой, диском, сетевой картой и прочей периферией через драйверы — специальные
          программы-переводчики между ОС и конкретным устройством.
        </Term>
        <Term name="Интерфейс пользователя">
          способ отдавать команды: командная строка (CLI, текстовые команды) и графическая оболочка (GUI,
          окна и мышь).
        </Term>
        <Term name="Безопасность и разграничение доступа">
          аутентификация пользователей (проверка «кто ты»), система прав (что тебе можно) и изоляция процессов.
        </Term>
      </section>

      {/* Ядро и режимы */}
      <section className="theory-section">
        <h2 className="theory-heading-2">3. Ядро ОС и режимы выполнения</h2>
        <p>
          Ядро (kernel) — это центральная часть ОС, которая всегда находится в памяти и обладает полным
          контролем над железом. Чтобы приложения не могли случайно (или намеренно) повредить систему,
          процессор работает в двух режимах привилегий.
        </p>
        <Term name="Режим ядра (kernel mode)">
          режим с полным доступом к любым командам процессора и всей памяти. В нём работает только само ядро
          и доверенный код (драйверы). Ошибка здесь может «уронить» всю систему.
        </Term>
        <Term name="Пользовательский режим (user mode)">
          ограниченный режим, в котором работают обычные приложения. Из него нельзя напрямую обращаться к
          железу — только через ядро.
        </Term>
        <Term name="Системный вызов (system call)">
          «официальная просьба» приложения к ядру выполнить привилегированную операцию: открыть файл, выделить
          память, отправить пакет в сеть. Это единственный легальный «мост» из user mode в kernel mode, и
          именно на этом мосту ядро проверяет права.
        </Term>
        <Fig caption="Разделение привилегий: приложение не трогает железо напрямую, а обращается к ядру через системные вызовы">
          <svg viewBox="0 0 560 220" width="100%" style={{ maxWidth: 560 }} xmlns="http://www.w3.org/2000/svg">
            <rect x="20" y="20" width="250" height="180" rx="8" fill="#242b3a" stroke={C.border} />
            <text x="145" y="42" fill={C.sub} fontSize="12" fontWeight="700" textAnchor="middle">USER MODE</text>
            <rect x="45" y="60" width="90" height="46" rx="6" fill="#1a1a24" stroke={C.border} />
            <text x="90" y="88" fill={C.text} fontSize="12" textAnchor="middle">Браузер</text>
            <rect x="155" y="60" width="90" height="46" rx="6" fill="#1a1a24" stroke={C.border} />
            <text x="200" y="88" fill={C.text} fontSize="12" textAnchor="middle">Редактор</text>
            <text x="145" y="150" fill={C.sub} fontSize="11" textAnchor="middle">ограниченный доступ</text>

            <rect x="330" y="20" width="210" height="180" rx="8" fill="rgba(255,214,10,0.1)" stroke={C.lime} />
            <text x="435" y="42" fill={C.lime} fontSize="12" fontWeight="700" textAnchor="middle">KERNEL MODE</text>
            <rect x="355" y="60" width="160" height="46" rx="6" fill="#1a1a24" stroke={C.lime} />
            <text x="435" y="88" fill={C.text} fontSize="12" textAnchor="middle">Ядро ОС</text>
            <text x="435" y="150" fill={C.sub} fontSize="11" textAnchor="middle">полный доступ к железу</text>

            {/* стрелка system call */}
            <line x1="270" y1="83" x2="330" y2="83" stroke={C.lime} strokeWidth="2" markerEnd="url(#arrow)" />
            <text x="300" y="76" fill={C.lime} fontSize="10" textAnchor="middle">syscall →</text>
            <defs>
              <marker id="arrow" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
                <path d="M0,0 L6,3 L0,6 Z" fill={C.lime} />
              </marker>
            </defs>
          </svg>
        </Fig>
      </section>

      {/* Типы ядра */}
      <section className="theory-section">
        <h2 className="theory-heading-2">4. Типы ядра</h2>
        <p>Ядра отличаются тем, сколько служб работает внутри привилегированного режима.</p>
        <Term name="Монолитное ядро">
          все службы (файловая система, драйверы, сеть) работают в едином адресном пространстве ядра. Быстро,
          но ошибка в любом драйвере может обрушить всю систему. Пример — классический Linux.
        </Term>
        <Term name="Микроядро">
          в ядре оставляют минимум (память, процессы, обмен сообщениями), а всё остальное выносят в отдельные
          изолированные процессы user mode. Надёжнее и безопаснее (сбой драйвера не роняет систему), но
          медленнее из-за обмена сообщениями.
        </Term>
        <Term name="Гибридное ядро">
          компромисс: основа монолитная, но часть служб изолирована. Так устроены ядро Windows NT и ядро XNU
          в macOS.
        </Term>
        <Fig caption="Монолит держит драйверы внутри ядра; микроядро выносит их наружу; гибрид — посередине">
          <svg viewBox="0 0 560 180" width="100%" style={{ maxWidth: 560 }} xmlns="http://www.w3.org/2000/svg">
            {/* monolithic */}
            <text x="93" y="20" fill={C.text} fontSize="12" fontWeight="700" textAnchor="middle">Монолитное</text>
            <rect x="20" y="30" width="146" height="130" rx="8" fill="rgba(255,214,10,0.08)" stroke={C.lime} />
            {['Планировщик','Память','Драйверы','Файл. система','Сеть'].map((t,i)=>(
              <text key={i} x="93" y={55+i*22} fill={C.sub} fontSize="11" textAnchor="middle">{t}</text>
            ))}
            {/* microkernel */}
            <text x="280" y="20" fill={C.text} fontSize="12" fontWeight="700" textAnchor="middle">Микроядро</text>
            <rect x="230" y="90" width="100" height="70" rx="8" fill="rgba(255,214,10,0.08)" stroke={C.lime} />
            <text x="280" y="120" fill={C.sub} fontSize="10" textAnchor="middle">Память</text>
            <text x="280" y="135" fill={C.sub} fontSize="10" textAnchor="middle">Процессы</text>
            {['Драйверы','Файлы','Сеть'].map((t,i)=>(
              <g key={i}>
                <rect x={225+i*38} y="30" width="34" height="30" rx="5" fill="#242b3a" stroke={C.border} />
                <text x={242+i*38} y="49" fill={C.sub} fontSize="8" textAnchor="middle">{t}</text>
              </g>
            ))}
            {/* hybrid */}
            <text x="470" y="20" fill={C.text} fontSize="12" fontWeight="700" textAnchor="middle">Гибридное</text>
            <rect x="410" y="60" width="120" height="100" rx="8" fill="rgba(255,214,10,0.08)" stroke={C.lime} />
            <text x="470" y="95" fill={C.sub} fontSize="10" textAnchor="middle">Ядро + часть</text>
            <text x="470" y="110" fill={C.sub} fontSize="10" textAnchor="middle">служб внутри</text>
            {['Часть','служб'].map((t,i)=>(
              <g key={i}>
                <rect x={425+i*55} y="30" width="50" height="24" rx="5" fill="#242b3a" stroke={C.border} />
                <text x={450+i*55} y="46" fill={C.sub} fontSize="9" textAnchor="middle">{t}</text>
              </g>
            ))}
          </svg>
        </Fig>
      </section>

      {/* Классификация */}
      <section className="theory-section">
        <h2 className="theory-heading-2">5. Классификация ОС по назначению</h2>
        <ul className="theory-list">
          <li><strong>Настольные (десктопные)</strong> — для конечного пользователя: Windows, macOS, Linux для ПК.</li>
          <li><strong>Серверные</strong> — под многопользовательскую нагрузку и сетевые службы: Windows Server, серверные Linux.</li>
          <li><strong>Мобильные</strong> — под ограниченные ресурсы и сенсор: Android, iOS.</li>
          <li><strong>Встраиваемые (embedded)</strong> — в IoT-устройствах и контроллерах.</li>
          <li><strong>Реального времени (RTOS)</strong> — с гарантированным временем реакции: промышленность, медицина.</li>
        </ul>
      </section>

      {/* Windows */}
      <section className="theory-section">
        <h2 className="theory-heading-2">6. Windows: архитектура и безопасность</h2>
        <p>Проприетарная ОС Microsoft на гибридном ядре NT. Разберём ключевые термины.</p>
        <Term name="Ядро NT (NT kernel)">
          сердце Windows. Отвечает за процессы, память, безопасность и обмен с драйверами. «NT» — New
          Technology; на этом ядре построены все современные версии Windows.
        </Term>
        <Term name="HAL (Hardware Abstraction Layer)">
          уровень абстракции оборудования — тонкая прослойка между ядром и конкретным железом. Благодаря HAL
          одно и то же ядро Windows работает на разных материнских платах и процессорах: детали железа спрятаны
          за единым интерфейсом.
        </Term>
        <Term name="Подсистемы Win32 / WinRT">
          наборы API (готовых функций), через которые приложения обращаются к Windows. Win32 — классический API
          для десктопных программ; WinRT (Windows Runtime) — более новый API для современных приложений. Приложение
          не знает про ядро напрямую — оно вызывает функции подсистемы, а та уже общается с ядром.
        </Term>
        <Term name="NTFS">
          файловая система Windows. «Журналируемая» — ведёт журнал операций, чтобы после сбоя восстановить
          целостность. Поддерживает ACL (списки прав доступа) и EFS (шифрование отдельных файлов).
        </Term>
        <Term name="ACL (Access Control List)">
          список, привязанный к файлу или папке, где для каждого пользователя/группы указано, что именно ему
          разрешено: чтение, запись, выполнение и т.д. Это гибче, чем простые права «владелец/группа/остальные».
        </Term>
        <Term name="Active Directory">
          служба каталогов для корпоративных сетей: централизованно хранит учётные записи, группы и политики.
          Позволяет администратору управлять тысячами компьютеров из одной точки (домена).
        </Term>
        <Term name="UAC (User Account Control)">
          контроль учётных записей: даже администратор работает с обычными правами, а при опасном действии
          система запрашивает подтверждение (всплывающее окно «Разрешить приложению внести изменения?»). Это
          мешает вредоносам тихо получить полные права.
        </Term>

        <h3 className="theory-heading-3">Средства защиты Windows — как они работают</h3>
        <Term name="Windows Defender">
          встроенный антивирус. Постоянно сканирует файлы и процессы, сравнивая их с базой сигнатур известных
          угроз, а также анализирует подозрительное поведение (эвристика). При обнаружении угрозы помещает файл
          в карантин.
        </Term>
        <Term name="Windows Firewall (брандмауэр)">
          сетевой фильтр. Проверяет весь входящий и исходящий сетевой трафик и по правилам решает, пропустить
          его или заблокировать (например, разрешить браузеру выходить в интернет, но запретить неизвестной
          программе принимать входящие соединения). Защищает от сетевых атак и «звонков домой» вредоносов.
        </Term>
        <Term name="BitLocker">
          шифрование всего диска. Все данные на диске хранятся в зашифрованном виде, а ключ обычно защищён
          аппаратным чипом TPM. Если ноутбук украдут и вынут диск — прочитать данные без ключа не получится.
        </Term>
        <Term name="Windows Hello">
          биометрическая аутентификация: вход по лицу, отпечатку пальца или PIN-коду вместо пароля.
          Биометрия хранится локально в защищённом виде и не передаётся в сеть, что безопаснее паролей,
          которые можно подсмотреть или подобрать.
        </Term>
      </section>

      {/* Linux */}
      <section className="theory-section">
        <h2 className="theory-heading-2">7. Linux: архитектура и безопасность</h2>
        <p>Семейство Unix-подобных ОС на открытом ядре Linux. Распространяется дистрибутивами (Ubuntu, Debian, Fedora…).</p>
        <Term name="Монолитное модульное ядро">
          ядро Linux монолитное (службы внутри), но модульное: драйверы можно подгружать и выгружать на лету
          в виде модулей (LKM, Loadable Kernel Modules), не перезагружая систему.
        </Term>
        <Term name="«Всё есть файл»">
          философия Unix/Linux: не только документы, но и устройства, процессы и даже сетевые сокеты
          представлены как файлы в едином дереве каталогов. Это упрощает и управление, и настройку прав.
        </Term>

        <h3 className="theory-heading-3">Права доступа rwx</h3>
        <p>
          Базовая модель прав Linux (её называют дискреционной, DAC): у каждого файла есть владелец и группа,
          и для трёх категорий — <strong>владелец / группа / остальные</strong> — заданы три права:
        </p>
        <Fig caption="Права rwx для файла: пример rwxr-x r-- — владелец читает/пишет/выполняет, группа читает/выполняет, остальные только читают">
          <svg viewBox="0 0 520 150" width="100%" style={{ maxWidth: 520 }} xmlns="http://www.w3.org/2000/svg">
            {[
              { x: 40,  cat: 'Владелец', bits: ['r','w','x'], on: [true,true,true] },
              { x: 210, cat: 'Группа',   bits: ['r','w','x'], on: [true,false,true] },
              { x: 380, cat: 'Остальные',bits: ['r','w','x'], on: [true,false,false] },
            ].map((g, gi) => (
              <g key={gi}>
                <text x={g.x + 50} y="28" fill={C.text} fontSize="12" fontWeight="700" textAnchor="middle">{g.cat}</text>
                {g.bits.map((b, i) => (
                  <g key={i}>
                    <rect x={g.x + i*35} y="40" width="30" height="30" rx="5"
                      fill={g.on[i] ? 'rgba(255,214,10,0.18)' : '#242b3a'} stroke={g.on[i] ? C.lime : C.border} />
                    <text x={g.x + i*35 + 15} y="60" fill={g.on[i] ? C.lime : C.sub} fontSize="14" fontWeight="700" textAnchor="middle">{g.on[i] ? b : '-'}</text>
                  </g>
                ))}
              </g>
            ))}
            <text x="60" y="105" fill={C.sub} fontSize="11">r = чтение (read)</text>
            <text x="230" y="105" fill={C.sub} fontSize="11">w = запись (write)</text>
            <text x="400" y="105" fill={C.sub} fontSize="11">x = выполнение</text>
          </svg>
        </Fig>
        <Term name="ACL в Linux">
          расширенные права поверх rwx: позволяют выдать доступ конкретному пользователю, не меняя владельца
          и группу файла — как ACL в Windows.
        </Term>

        <h3 className="theory-heading-3">Мандатный контроль доступа: SELinux и AppArmor</h3>
        <Term name="Дискреционный контроль (DAC) vs мандатный (MAC)">
          при DAC (обычные права rwx) владелец файла сам решает, кому дать доступ — «на своё усмотрение».
          При мандатном контроле (MAC) правила задаёт администратор через политику, и их не может обойти даже
          владелец или root. MAC — более строгий уровень: даже если процесс захвачен злоумышленником, политика
          ограничит, что он вообще может сделать.
        </Term>
        <Term name="SELinux">
          система мандатного контроля (изначально от АНБ). Каждому процессу и файлу присваивается метка
          (контекст безопасности), а централизованная политика описывает, какой контекст с каким может
          взаимодействовать. Например, веб-сервер помечен так, что физически не может читать файлы паролей,
          даже работая от root.
        </Term>
        <Term name="AppArmor">
          более простая альтернатива SELinux. Вместо меток использует профили по пути к программе: для каждого
          приложения описан белый список — к каким файлам и возможностям оно имеет доступ. Всё, что не разрешено
          профилем, блокируется.
        </Term>

        <h3 className="theory-heading-3">Сетевой экран: iptables и nftables</h3>
        <Term name="iptables / nftables">
          инструменты управления встроенным сетевым фильтром Linux (подсистема netfilter в ядре). Задают
          правила: какие сетевые пакеты пропускать, а какие отбрасывать. Правила объединены в цепочки (chains) —
          входящие (INPUT), исходящие (OUTPUT), транзитные (FORWARD). nftables — современная замена iptables с
          более удобным синтаксисом. По сути это и есть файрвол уровня ОС.
        </Term>

        <h3 className="theory-heading-3">Повышение прав: sudo</h3>
        <Term name="sudo (superuser do)">
          команда, позволяющая обычному пользователю выполнить одну операцию с правами администратора (root),
          введя свой пароль. Это безопаснее, чем постоянно работать под root: права повышаются только на
          конкретную команду и только тем, кому это явно разрешено в файле настроек (sudoers). Плюс все вызовы
          sudo логируются — видно, кто и что делал.
        </Term>
      </section>

      {/* macOS */}
      <section className="theory-section">
        <h2 className="theory-heading-2">8. macOS: архитектура и безопасность</h2>
        <p>Проприетарная ОС Apple на гибридном ядре XNU.</p>
        <Term name="Ядро XNU">
          «X is Not Unix». Гибрид: сочетает микроядро Mach (управление памятью и процессами, обмен сообщениями)
          и компоненты BSD (Unix-подсистема, сеть, файловая система, права).
        </Term>
        <Term name="Darwin">
          открытая Unix-основа macOS: ядро XNU плюс базовые системные утилиты. Именно поэтому в терминале macOS
          доступны привычные Unix-команды.
        </Term>
        <Term name="Cocoa">
          закрытые графические фреймворки Apple, на которых пишут приложения с «маковским» интерфейсом.
        </Term>
        <Term name="APFS (Apple File System)">
          файловая система, оптимизированная под SSD: поддерживает шифрование «из коробки» и снапшоты (мгновенные
          снимки состояния диска для отката).
        </Term>
        <Term name="Gatekeeper">
          контроль источников ПО: разрешает запускать только приложения от проверенных разработчиков (с цифровой
          подписью Apple). Скачанное из непроверенного места приложение по умолчанию не запустится.
        </Term>
        <Term name="SIP (System Integrity Protection)">
          защита целостности системы: запрещает изменять системные файлы и папки даже пользователю root. Это
          мешает вредоносам «прописаться» в системе.
        </Term>
        <Term name="Песочница (sandbox)">
          изоляция приложений: каждое приложение работает в ограниченном «загоне» и имеет доступ только к своим
          данным и к тому, что явно разрешил пользователь (например, к камере или папке).
        </Term>
      </section>

      {/* Сравнение */}
      <section className="theory-section">
        <h2 className="theory-heading-2">9. Сравнение: Windows, Linux, macOS</h2>
        <TheoryTable
          headers={['Критерий', 'Windows', 'Linux', 'macOS']}
          rows={[
            ['Тип ядра', 'Гибридное (NT)', 'Монолитное модульное', 'Гибридное (XNU)'],
            ['Лицензия', 'Проприетарная', 'Открытый исходный код', 'Проприетарная'],
            ['Файловая система', 'NTFS', 'ext4 / XFS / Btrfs', 'APFS'],
            ['Модель прав', 'ACL, UAC', 'rwx, SELinux/AppArmor (MAC)', 'Unix-права, SIP'],
            ['Сетевой экран', 'Windows Firewall', 'iptables / nftables', 'встроенный (pf)'],
            ['Основная сфера', 'Офис, корпорации, игры', 'Серверы, embedded, DevOps', 'Творческие и dev-задачи'],
          ]}
        />
        <TheoryExample title="Вывод для безопасника">
          Открытость кода Linux позволяет аудитировать систему и тонко настраивать мандатный контроль
          (SELinux/AppArmor) — поэтому Linux доминирует на серверах и в пентесте. Windows с ACL и Active
          Directory удобна для управления большими корпоративными сетями, но её массовость делает её частой
          мишенью. macOS ставит на строгую изоляцию приложений (SIP, sandbox, Gatekeeper).
        </TheoryExample>
      </section>

      {/* Выводы */}
      <section className="theory-section">
        <h2 className="theory-heading-2">10. Выводы</h2>
        <ul className="theory-list">
          <li>ОС — посредник между железом и приложениями; на её уровне закладывается базовая безопасность.</li>
          <li>Разделение kernel/user mode и системные вызовы — фундаментальный механизм защиты от вредоносного и ошибочного кода.</li>
          <li>Тип ядра (монолитное, микроядро, гибридное) определяет баланс между скоростью и надёжностью.</li>
          <li>Windows: ядро NT, HAL, ACL и Active Directory; защита — Defender, Firewall, BitLocker, Windows Hello.</li>
          <li>Linux: открытое монолитное ядро, права rwx, мандатный контроль SELinux/AppArmor, файрвол iptables/nftables, повышение прав через sudo.</li>
          <li>macOS: ядро XNU (Mach + BSD), APFS, строгая изоляция приложений через SIP, Gatekeeper и sandbox.</li>
          <li>Выбор ОС в задачах безопасности определяется моделью прав доступа, открытостью кода и сферой применения.</li>
        </ul>
      </section>
    </div>
  )
}
