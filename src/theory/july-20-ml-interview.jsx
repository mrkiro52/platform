import { TheoryCode, TheoryExample } from './components/TheoryTable'

// Карточка вопрос-ответ для формата собеседования
function QA({ n, q, children }) {
  return (
    <div style={{ margin: '20px 0', paddingBottom: 18, borderBottom: '1px solid var(--border-color)' }}>
      <div style={{ display: 'flex', gap: 10, alignItems: 'flex-start', marginBottom: 8 }}>
        <span style={{
          flexShrink: 0, width: 24, height: 24, borderRadius: '50%', background: 'var(--accent-lime)',
          color: '#0a0a14', fontSize: 12, fontWeight: 800, display: 'flex', alignItems: 'center',
          justifyContent: 'center', marginTop: 2,
        }}>{n}</span>
        <p style={{ margin: 0, fontWeight: 700, color: 'var(--text-primary)', fontSize: 16 }}>{q}</p>
      </div>
      <div style={{ marginLeft: 34, color: 'var(--text-secondary)', fontSize: 14.5, lineHeight: 1.75 }}>
        {children}
      </div>
    </div>
  )
}

function Section({ title }) {
  return <h2 className="theory-heading-2">{title}</h2>
}

export default function July20MlInterviewTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Вопросы с собеседования ML: Python и основы</h1>
        <p className="theory-subtitle">Трек: Machine Learning</p>
        <p className="theory-date">20 июля 2026</p>
        <p>
          Сегодня — не новая тема, а тренировка на реальных вопросах, которые звучат на собеседованиях на позиции,
          связанные с ML и анализом данных. Первый блок — вопросы, которые реально прозвучали на одном из
          собеседований (язык Python, ООП, структуры данных). Второй блок — ещё около 20 классических вопросов по
          Python, которые часто спрашивают вместе с ними. Формат — вопрос и развёрнутый ответ, как на самом
          собеседовании.
        </p>
      </section>

      <section className="theory-section">
        <Section title="Часть 1. Вопросы с реального собеседования" />

        <QA n={1} q="Что такое Python?">
          Python — высокоуровневый интерпретируемый язык программирования общего назначения с динамической
          типизацией и автоматическим управлением памятью. Отличается простым и читаемым синтаксисом, огромной
          экосистемой библиотек (NumPy, pandas, scikit-learn, PyTorch и т.д.), из-за чего стал основным языком в
          сфере анализа данных и машинного обучения.
        </QA>

        <QA n={2} q="Какие основные типы данных есть в Python?">
          Числовые: <code>int</code>, <code>float</code>, <code>complex</code>. Логический: <code>bool</code>.
          Последовательности: <code>str</code>, <code>list</code>, <code>tuple</code>. Множества:{' '}
          <code>set</code>, <code>frozenset</code>. Отображения: <code>dict</code>. Отдельно — <code>None</code> как
          обозначение отсутствия значения.
        </QA>

        <QA n={3} q="Чем list отличается от tuple?">
          <code>list</code> — изменяемая (mutable) последовательность: элементы можно добавлять, удалять, менять.{' '}
          <code>tuple</code> — неизменяемая (immutable): после создания её содержимое нельзя поменять. Из этого
          следует практическая разница: tuple можно использовать как ключ словаря или элемент множества (если внутри
          только неизменяемые объекты), list — нельзя. Tuple также занимает чуть меньше памяти и создаётся быстрее.
        </QA>

        <QA n={4} q="Что такое list comprehension?">
          Компактный синтаксис для создания списка на основе цикла и условия в одну строку, без явного{' '}
          <code>for</code> с <code>append</code>.
          <TheoryCode language="python" code={`squares = [x * x for x in range(10)]
# [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]

# то же самое обычным циклом:
squares = []
for x in range(10):
    squares.append(x * x)`} />
        </QA>

        <QA n={5} q="Что делает enumerate()?">
          Оборачивает любой перебираемый объект (список, строку и т.д.) так, что при переборе в цикле сразу
          доступны и индекс элемента, и сам элемент — не нужно заводить отдельный счётчик.
          <TheoryCode language="python" code={`for i, name in enumerate(["Аня", "Боря", "Вика"]):
    print(i, name)
# 0 Аня
# 1 Боря
# 2 Вика`} />
        </QA>

        <QA n={6} q="Что такое *args и **kwargs?">
          Способ передать в функцию произвольное количество аргументов. <code>*args</code> собирает позиционные
          аргументы в кортеж, <code>**kwargs</code> — именованные аргументы в словарь.
          <TheoryCode language="python" code={`def show(*args, **kwargs):
    print(args)    # (1, 2, 3)
    print(kwargs)  # {'name': 'Аня', 'age': 20}

show(1, 2, 3, name="Аня", age=20)`} />
        </QA>

        <QA n={7} q="Что такое исключения в Python?">
          Механизм обработки ошибок времени выполнения: вместо аварийного завершения программы ошибку можно
          «поймать» и обработать. Конструкция <code>try / except / finally</code> — <code>try</code> содержит код,
          который может упасть, <code>except</code> ловит конкретный тип ошибки, <code>finally</code> выполняется
          всегда, независимо от того, была ошибка или нет.
          <TheoryCode language="python" code={`try:
    result = 10 / 0
except ZeroDivisionError:
    print("На ноль делить нельзя!")
finally:
    print("Этот блок выполнится в любом случае")`} />
        </QA>

        <QA n={8} q="Что делает garbage collector (сборщик мусора)?">
          Автоматически освобождает память, занятую объектами, на которые больше нет ссылок в программе. В Python
          основной механизм — подсчёт ссылок (reference counting): как только счётчик ссылок на объект падает до
          нуля, память освобождается сразу же. Дополнительно есть отдельный сборщик для поиска циклических ссылок
          (когда объекты ссылаются друг на друга, но недоступны извне) — он проходит периодически.
        </QA>

        <QA n={9} q="Что такое класс?">
          «Чертёж» — шаблон, описывающий, какие данные (атрибуты) и поведение (методы) будут у объектов этого типа.
          Класс сам по себе не хранит конкретные данные, а описывает структуру, по которой создают объекты.
        </QA>

        <QA n={10} q="Что такое объект?">
          Конкретный экземпляр, созданный по шаблону класса, с реальными значениями атрибутов. Из одного класса{' '}
          <code>Car</code> можно создать множество разных объектов-автомобилей с разными значениями свойств.
        </QA>

        <QA n={11} q="Что такое наследование?">
          Механизм, позволяющий одному классу (потомку) взять свойства и методы другого класса (родителя) и
          дополнить или переопределить их. Избавляет от дублирования общего кода между похожими сущностями.
        </QA>

        <QA n={12} q="Что такое инкапсуляция?">
          Сокрытие внутреннего состояния объекта — наружу открывают только то, что нужно для использования, а
          детали реализации прячут от прямого доступа снаружи. В Python приватность условная, обозначается через
          нижнее подчёркивание.
          <TheoryCode language="python" code={`class Account:
    def __init__(self):
        self._balance = 100   # "приватный" атрибут (соглашение)

    def deposit(self, amount):
        self._balance += amount

    def get_balance(self):
        return self._balance`} />
        </QA>

        <QA n={13} q="Что такое полиморфизм?">
          Способность объектов разных классов отвечать на один и тот же вызов метода по-своему. Позволяет
          обрабатывать разнородные объекты единым кодом, не зная заранее, с каким конкретно классом имеем дело —
          например, вызывать <code>.speak()</code> у списка из разных животных, и каждое «говорит» по-своему.
        </QA>

        <QA n={14} q="Что такое абстрактный класс и абстракция в ООП?">
          Абстракция — выделение существенных характеристик объекта и сокрытие деталей реализации. Абстрактный
          класс — класс, который нельзя создать напрямую: он задаёт «контракт» (обязательные методы), которые
          обязаны реализовать все классы-потомки, но сам не знает, как именно это будет сделано.
          <TheoryCode language="python" code={`from abc import ABC, abstractmethod

class Shape(ABC):
    @abstractmethod
    def area(self):
        pass   # контракт: каждый наследник обязан реализовать area()

class Circle(Shape):
    def __init__(self, r):
        self.r = r
    def area(self):
        return 3.14159 * self.r ** 2`} />
        </QA>

        <QA n={15} q="Что такое dunder-методы?">
          Dunder — сокращение от «double underscore» (двойное подчёркивание). Это специальные методы вида{' '}
          <code>__имя__</code>, которые Python вызывает автоматически в определённых ситуациях: <code>__init__</code>{' '}
          — при создании объекта, <code>__str__</code> — при вызове <code>str()</code>/<code>print()</code>,{' '}
          <code>__repr__</code> — при отладочном представлении объекта, <code>__add__</code> — при использовании{' '}
          <code>+</code>, <code>__len__</code> — при вызове <code>len()</code>. Благодаря им пользовательские
          объекты могут вести себя как встроенные типы.
        </QA>

        <QA n={16} q="Чем отличается == от is?">
          <code>==</code> сравнивает <strong>значения</strong> двух объектов (равны ли они по содержимому).{' '}
          <code>is</code> сравнивает <strong>идентичность</strong> — один ли это объект в памяти (одинаковый ли{' '}
          <code>id()</code>). Два разных списка с одинаковым содержимым равны по <code>==</code>, но не равны по{' '}
          <code>is</code>, потому что это разные объекты в памяти.
        </QA>

        <QA n={17} q="Как устроен словарь Python изнутри?">
          Словарь — это хеш-таблица: каждый ключ прогоняется через хеш-функцию, которая превращает его в число
          (хеш), и по этому числу вычисляется индекс ячейки во внутреннем массиве, куда кладётся пара ключ-значение.
          Благодаря этому доступ к элементу по ключу в среднем занимает O(1) — не важно, сколько всего элементов в
          словаре.
        </QA>

        <QA n={18} q="Что такое коллизия в словаре?">
          Ситуация, когда хеш-функция выдаёт одинаковый (или попадающий в одну и ту же ячейку) хеш для двух разных
          ключей. Python разрешает такие коллизии автоматически (например, методом открытой адресации — ищет
          следующую свободную ячейку), но чем больше коллизий, тем медленнее становится доступ к элементам.
        </QA>

        <QA n={19} q="Какой поиск элемента будет быстрее: x in list или x in set — и какая у них Big O?">
          <code>x in list</code> — это O(n): Python последовательно проверяет каждый элемент списка, пока не найдёт
          совпадение или не дойдёт до конца. <code>x in set</code> — это O(1) в среднем: множество, как и словарь,
          построено на хеш-таблице, поэтому проверка на принадлежность идёт напрямую по хешу, без перебора. Вывод:
          для частых проверок «есть ли элемент» гораздо выгоднее хранить данные в <code>set</code>, а не в{' '}
          <code>list</code>.
        </QA>

        <QA n={20} q="Что делают pass, break и continue?">
          <code>pass</code> — «ничего не делать», заглушка там, где по синтаксису нужен блок кода (например, в
          пустом теле функции или класса). <code>break</code> — немедленно прерывает выполнение всего цикла.{' '}
          <code>continue</code> — прерывает только текущую итерацию цикла и переходит к следующей, не прерывая цикл
          целиком.
        </QA>
      </section>

      <section className="theory-section">
        <Section title="Часть 2. Ещё 20 классических вопросов по Python" />

        <QA n={21} q="В чём разница между интерпретируемым и компилируемым языком?">
          У компилируемого языка (C, Go) исходный код заранее полностью переводится в машинный код, и запускается
          уже готовый исполняемый файл. У интерпретируемого языка (Python) код выполняется построчно специальной
          программой-интерпретатором во время запуска. На практике Python сначала компилирует код в байт-код (файлы{' '}
          <code>.pyc</code>), а затем этот байт-код выполняется интерпретатором — то есть формально это гибрид.
        </QA>

        <QA n={22} q="Что такое PEP 8?">
          Официальный документ (Python Enhancement Proposal №8) с рекомендациями по стилю кода на Python: отступы в
          4 пробела, длина строки, именование переменных и функций (snake_case), расположение импортов и так далее.
          Следование PEP 8 делает код единообразным и понятным для других разработчиков.
        </QA>

        <QA n={23} q="Что такое срезы (slices) в Python?">
          Способ получить часть последовательности (строки, списка, кортежа) по синтаксису{' '}
          <code>[начало:конец:шаг]</code>.
          <TheoryCode language="python" code={`a = [0, 1, 2, 3, 4, 5]
a[1:4]     # [1, 2, 3]
a[:3]      # [0, 1, 2]
a[::2]     # [0, 2, 4]  — каждый второй элемент
a[::-1]    # [5, 4, 3, 2, 1, 0]  — разворот последовательности`} />
        </QA>

        <QA n={24} q="Чем изменяемые (mutable) типы отличаются от неизменяемых (immutable)?">
          Изменяемые объекты (list, dict, set) можно менять «на месте» после создания — их <code>id()</code>{' '}
          останется тем же. Неизменяемые (int, str, tuple, frozenset) при попытке «изменить» на самом деле создают
          новый объект в памяти. Это важно при передаче объектов в функции и при использовании их как ключей
          словаря (ключом может быть только неизменяемый объект).
        </QA>

        <QA n={25} q="Что такое GIL (Global Interpreter Lock)?">
          Глобальная блокировка интерпретатора в CPython, которая разрешает выполнять байт-код Python только одному
          потоку в один момент времени, даже на многоядерном процессоре. Из-за GIL многопоточность в Python плохо
          ускоряет CPU-интенсивные задачи, но не мешает ускорять I/O-задачи (сеть, диск), потому что во время
          ожидания I/O поток отдаёт GIL другим потокам.
        </QA>

        <QA n={26} q="В чём разница между процессом и потоком?">
          Процесс — независимая программа со своим собственным выделенным участком памяти. Поток — «нить»
          выполнения внутри процесса, потоки одного процесса делят общую память между собой. Из-за GIL для
          настоящего параллелизма CPU-задач в Python чаще используют multiprocessing (несколько процессов), а не
          threading (несколько потоков).
        </QA>

        <QA n={27} q="Что такое декоратор в Python?">
          Функция, которая принимает другую функцию и возвращает изменённую версию этой функции, добавляя новое
          поведение без изменения её исходного кода.
          <TheoryCode language="python" code={`def log_call(func):
    def wrapper(*args, **kwargs):
        print(f"Вызывается {func.__name__}")
        return func(*args, **kwargs)
    return wrapper

@log_call
def greet(name):
    print(f"Привет, {name}!")

greet("Аня")
# Вызывается greet
# Привет, Аня!`} />
        </QA>

        <QA n={28} q="Что такое генератор (generator) и чем он отличается от списка?">
          Генератор — объект, который выдаёт значения по одному, по требованию, а не хранит все значения в памяти
          сразу, как список. Создаётся через функцию с <code>yield</code> или через генераторное выражение{' '}
          <code>(x for x in ...)</code>. Экономит память на больших или бесконечных последовательностях, но
          значения можно перебрать только один раз.
          <TheoryCode language="python" code={`def gen_squares(n):
    for x in range(n):
        yield x * x

for val in gen_squares(5):
    print(val)   # 0 1 4 9 16 — по одному, без хранения всего списка`} />
        </QA>

        <QA n={29} q="Что такое контекстный менеджер и конструкция with?">
          Механизм, гарантирующий, что ресурс (файл, соединение) будет корректно закрыт после использования, даже
          если внутри блока произошла ошибка. Реализуется через dunder-методы <code>__enter__</code> и{' '}
          <code>__exit__</code>.
          <TheoryCode language="python" code={`with open("file.txt") as f:
    data = f.read()
# файл автоматически закроется здесь, даже если read() вызвал ошибку`} />
        </QA>

        <QA n={30} q="Что такое lambda-функция?">
          Анонимная (безымянная) функция, определяемая в одну строку без ключевого слова <code>def</code>. Обычно
          используется там, где нужна короткая функция «на один раз» — например, как аргумент для{' '}
          <code>sorted()</code> или <code>map()</code>.
          <TheoryCode language="python" code={`square = lambda x: x * x
square(5)   # 25

names = ["Вика", "Аня", "Боря"]
sorted(names, key=lambda name: len(name))`} />
        </QA>

        <QA n={31} q="Чем map(), filter() и reduce() отличаются друг от друга?">
          <code>map(func, iterable)</code> применяет функцию к каждому элементу и возвращает результаты.{' '}
          <code>filter(func, iterable)</code> оставляет только те элементы, для которых функция вернула{' '}
          <code>True</code>. <code>reduce(func, iterable)</code> (из модуля <code>functools</code>) последовательно
          «сворачивает» все элементы в одно значение (например, сумму или произведение).
        </QA>

        <QA n={32} q="Что такое shallow copy и deep copy?">
          Shallow copy (поверхностная копия, <code>copy.copy()</code>) создаёт новый объект верхнего уровня, но
          вложенные объекты внутри остаются общими с оригиналом — изменение вложенного списка отразится в обеих
          копиях. Deep copy (<code>copy.deepcopy()</code>) рекурсивно копирует и все вложенные объекты — копии
          становятся полностью независимыми.
        </QA>

        <QA n={33} q="Что произойдёт при передаче списка в функцию и его изменении внутри?">
          Python передаёт аргументы «по ссылке на объект»: если внутри функции изменить содержимое изменяемого
          объекта (например, через <code>list.append()</code>), это изменение будет видно и снаружи функции, потому
          что и снаружи, и внутри — ссылка на один и тот же объект в памяти. А вот присвоение параметру нового
          значения (<code>param = []</code>) создаёт новую локальную ссылку и не влияет на объект снаружи.
        </QA>

        <QA n={34} q="Что такое множественное наследование и MRO?">
          Множественное наследование — когда класс наследуется сразу от нескольких родительских классов. MRO
          (Method Resolution Order) — порядок, в котором Python ищет метод среди цепочки родителей при вызове,
          вычисляется по алгоритму C3-линеаризации. Посмотреть порядок можно через{' '}
          <code>ClassName.__mro__</code>.
        </QA>

        <QA n={35} q="Что такое property (свойство) в Python?">
          Декоратор <code>@property</code> позволяет обращаться к методу класса как к обычному атрибуту, без
          круглых скобок вызова — удобно, если нужно добавить логику (например, валидацию) при чтении или записи
          «поля», не меняя внешний интерфейс использования класса.
          <TheoryCode language="python" code={`class Circle:
    def __init__(self, radius):
        self._radius = radius

    @property
    def area(self):
        return 3.14159 * self._radius ** 2

c = Circle(5)
print(c.area)   # обращаемся как к атрибуту, хотя это метод`} />
        </QA>

        <QA n={36} q="Как в Python работает управление памятью для объектов?">
          Каждый объект имеет счётчик ссылок (reference count) — сколько мест в коде на него ссылаются. Когда
          счётчик доходит до нуля, память объекта немедленно освобождается. Отдельно работает циклический сборщик
          мусора, который время от времени ищет и удаляет группы объектов, ссылающихся друг на друга по кругу, но
          не достижимых извне программы.
        </QA>

        <QA n={37} q="Что такое виртуальное окружение (venv) и зачем оно нужно?">
          Изолированная среда со своим собственным набором установленных библиотек, независимая от системного
          Python и от других проектов. Позволяет разным проектам использовать разные (и даже конфликтующие) версии
          одних и тех же библиотек, не мешая друг другу. Создаётся командой <code>python -m venv env</code>.
        </QA>

        <QA n={38} q="Чем модуль отличается от пакета в Python?">
          Модуль — это один файл <code>.py</code> с кодом, который можно импортировать. Пакет — это папка с
          несколькими модулями внутри и файлом <code>__init__.py</code>, помечающим её как импортируемый пакет.
          Пакеты позволяют логически группировать связанные модули.
        </QA>

        <QA n={39} q="Что такое побитовые операции и для чего они применяются?">
          Операции, работающие напрямую с битовым представлением чисел: <code>&amp;</code> (AND), <code>|</code>{' '}
          (OR), <code>^</code> (XOR), <code>~</code> (NOT), <code>&lt;&lt;</code> и <code>&gt;&gt;</code> (сдвиги).
          Применяются там, где важна максимальная скорость и компактность — работа с флагами, масками доступа,
          некоторыми алгоритмическими трюками (например, поиск уникального числа через XOR).
        </QA>

        <QA n={40} q="В чём разница между статическим методом, методом класса и обычным методом?">
          Обычный метод принимает <code>self</code> и работает с конкретным объектом. Метод класса (декоратор{' '}
          <code>@classmethod</code>) принимает <code>cls</code> вместо <code>self</code> и работает с самим классом,
          а не с объектом — часто используется как альтернативный конструктор. Статический метод (декоратор{' '}
          <code>@staticmethod</code>) не получает ни <code>self</code>, ни <code>cls</code> — это просто функция,
          логически связанная с классом, но не обращающаяся ни к объекту, ни к классу напрямую.
        </QA>

        <TheoryExample title="Как готовиться дальше">
          Не заучивайте ответы дословно — объясняйте своими словами и старайтесь сразу приводить короткий пример
          кода, как в этом конспекте. На собеседовании гораздо важнее показать понимание идеи, чем процитировать
          формулировку.
        </TheoryExample>
      </section>
    </div>
  )
}
