import { TheoryCode, TheoryExample } from './components/TheoryTable'

function QA({ n, q, children }) {
  return (
    <div style={{ margin: '20px 0', paddingBottom: 18, borderBottom: '1px solid var(--border-color)' }}>
      <div style={{ display: 'flex', gap: 10, alignItems: 'flex-start', marginBottom: 8 }}>
        <span style={{
          flexShrink: 0, width: 24, height: 24, borderRadius: '50%', background: 'var(--accent-lime)',
          color: '#0a0a14', fontSize: 12, fontWeight: 800, display: 'flex', alignItems: 'center',
          justifyContent: 'center', marginTop: 2,
        }}>{n}</span>
        <p style={{ margin: 0, fontWeight: 700, color: 'var(--text-primary)', fontSize: 16 }}>{q}</p>
      </div>
      <div style={{ marginLeft: 34, color: 'var(--text-secondary)', fontSize: 14.5, lineHeight: 1.75 }}>
        {children}
      </div>
    </div>
  )
}

function Section({ title }) {
  return <h2 className="theory-heading-2">{title}</h2>
}

export default function July21AnalyticsInterviewTheory() {
  return (
    <div className="theory-container">
      <section className="theory-section">
        <h1 className="theory-title">Вопросы с собеседования: Аналитика</h1>
        <p className="theory-subtitle">Трек: Аналитика</p>
        <p className="theory-date">21 июля 2026</p>
        <p>
          Тренировка на самых популярных вопросах, которые звучат на собеседованиях на позиции аналитика данных и
          продуктового аналитика. Первый блок — общие вопросы про метрики, эксперименты и статистику. Второй блок
          — вопросы по SQL, без которого не обходится ни одно собеседование аналитика. Формат — вопрос и
          развёрнутый ответ.
        </p>
      </section>

      <section className="theory-section">
        <Section title="Часть 1. Метрики, эксперименты, статистика" />

        <QA n={1} q="Чем занимается аналитик данных?">
          Аналитик данных собирает, обрабатывает и интерпретирует данные, чтобы помочь бизнесу принимать решения:
          считает метрики, строит отчёты и дашборды, проверяет гипотезы через A/B-тесты, ищет закономерности и
          объясняет их на понятном для нетехнических коллег языке.
        </QA>

        <QA n={2} q="Что такое A/B-тестирование?">
          Эксперимент, в котором пользователей случайно делят на контрольную группу (видит текущую версию) и
          экспериментальную (видит изменение), а затем сравнивают ключевую метрику между группами, чтобы понять,
          действительно ли изменение повлияло на неё, а не сработала случайность.
        </QA>

        <QA n={3} q="Что такое p-value и как его интерпретировать?">
          Вероятность получить наблюдаемую (или ещё более выраженную) разницу между группами при условии, что на
          самом деле никакой разницы нет (верна нулевая гипотеза). Если p-value меньше выбранного порога значимости
          (обычно 0.05), разницу считают статистически значимой — то есть маловероятно, что она случайна.
        </QA>

        <QA n={4} q="Что такое доверительный интервал?">
          Диапазон значений, в котором с заданной вероятностью (обычно 95%) находится истинное значение метрики
          (например, реальная конверсия). Чем уже интервал, тем точнее оценка; узкий интервал обычно означает
          больший размер выборки.
        </QA>

        <QA n={5} q="Когда медиана информативнее среднего?">
          Когда в данных есть выбросы или сильная асимметрия. Например, средняя зарплата в компании может быть
          сильно завышена зарплатой одного топ-менеджера, а медиана покажет типичное значение, на которое выбросы
          почти не влияют.
        </QA>

        <QA n={6} q="Чем корреляция отличается от причинности?">
          Корреляция показывает, что две величины изменяются согласованно, но не объясняет, почему. Причинность
          означает, что изменение одной величины действительно вызывает изменение другой. Классический пример:
          продажи мороженого и число утоплений коррелируют летом, но одно не вызывает другое — обе величины растут
          из-за жары (третьего, скрытого фактора).
        </QA>

        <QA n={7} q="Что такое выброс (outlier) и как его искать?">
          Значение, сильно выбивающееся из общего распределения данных (например, возраст 200 лет). Ищут через
          визуализацию (boxplot), правило трёх стандартных отклонений от среднего, или через межквартильный размах
          (IQR): значения за пределами Q1 − 1.5·IQR и Q3 + 1.5·IQR считают подозрительными.
        </QA>

        <QA n={8} q="Что такое воронка продаж (funnel)?">
          Последовательность шагов, которые проходит пользователь от первого касания с продуктом до целевого
          действия (покупки, регистрации). Анализ воронки показывает, на каком шаге теряется больше всего
          пользователей — именно этот шаг и стоит оптимизировать в первую очередь.
        </QA>

        <QA n={9} q="Что такое когортный анализ?">
          Метод, при котором пользователей группируют по общему признаку (обычно по дате первого визита или
          регистрации — «когорта») и отслеживают их поведение во времени отдельно по каждой группе. Позволяет
          увидеть, как меняется удержание или доход в зависимости от того, когда пользователь пришёл.
        </QA>

        <QA n={10} q="Что такое retention rate (удержание)?">
          Доля пользователей, которые вернулись в продукт спустя определённый период после первого визита
          (например, retention на 7-й день). Ключевая метрика для понимания, насколько продукт «залипателен» и
          решает реальную проблему пользователя.
        </QA>

        <QA n={11} q="Что такое LTV (Lifetime Value)?">
          Суммарная прибыль, которую в среднем приносит один пользователь за всё время использования продукта.
          Сравнивается со стоимостью привлечения (CAC) — здоровый бизнес обычно требует, чтобы LTV заметно
          превышал CAC.
        </QA>

        <QA n={12} q="Что такое CAC (Customer Acquisition Cost)?">
          Средняя стоимость привлечения одного нового платящего пользователя — все затраты на маркетинг и продажи
          за период, делённые на число новых клиентов за этот период.
        </QA>

        <QA n={13} q="В чём разница между ошибками первого и второго рода?">
          Ошибка первого рода (ложноположительная) — мы посчитали эффект значимым, хотя на самом деле его нет.
          Ошибка второго рода (ложноотрицательная) — мы не заметили реальный эффект, хотя он есть. Уровень
          значимости alpha контролирует вероятность первой ошибки, мощность теста — защищает от второй.
        </QA>

        <QA n={14} q="Зачем аналитику важно нормальное распределение?">
          Многие статистические тесты (например, t-тест) опираются на предположение о нормальности данных или
          хотя бы их средних значений (это гарантирует центральная предельная теорема при достаточно большой
          выборке). Понимание формы распределения помогает выбрать правильный статистический метод и корректно
          интерпретировать среднее, стандартное отклонение и выбросы.
        </QA>

        <QA n={15} q="Что означает «статистическая значимость» результата?">
          То, что наблюдаемая разница между группами вряд ли объясняется случайностью — при заданном уровне
          доверия. Важно помнить: статистическая значимость не равна практической значимости — разница может быть
          значимой, но слишком маленькой, чтобы иметь смысл менять продукт.
        </QA>

        <QA n={16} q="Как объяснить сложную метрику нетехническому стейкхолдеру?">
          Начать с бизнес-смысла метрики («это показывает, сколько людей возвращается к нам через неделю»), а не
          с формулы расчёта. Использовать конкретные цифры и сравнения («выросло с 20% до 25%, это на 1200
          пользователей больше»), избегать терминов вроде «p-value» и «дисперсия» без пояснений, и сразу говорить,
          что это значит для бизнеса и что можно с этим сделать.
        </QA>

        <TheoryExample title="Совет по подготовке">
          На собеседовании аналитика часто просят не просто дать определение, а разобрать на примере: «представь,
          что метрика упала — как будешь искать причину?». Тренируйся строить такие мини-кейсы вслух для каждого
          понятия из этого списка.
        </TheoryExample>
      </section>

      <section className="theory-section">
        <Section title="Часть 2. SQL для аналитика" />

        <QA n={17} q="Для чего нужен GROUP BY?">
          Группирует строки таблицы по значению одного или нескольких столбцов, чтобы применить к каждой группе
          агрегатную функцию — например, посчитать количество заказов или сумму выручки по каждому городу
          отдельно.
          <TheoryCode language="sql" code={`SELECT city, COUNT(*) AS orders_count, SUM(amount) AS total
FROM orders
GROUP BY city;`} />
        </QA>

        <QA n={18} q="В чём разница между WHERE и HAVING?">
          <code>WHERE</code> фильтрует отдельные строки <strong>до</strong> группировки. <code>HAVING</code>{' '}
          фильтрует уже готовые группы <strong>после</strong> агрегации — например, оставляет только те города, где
          суммарная выручка больше миллиона.
          <TheoryCode language="sql" code={`SELECT city, SUM(amount) AS total
FROM orders
WHERE status = 'paid'      -- фильтр строк ДО группировки
GROUP BY city
HAVING SUM(amount) > 1000000;  -- фильтр групп ПОСЛЕ агрегации`} />
        </QA>

        <QA n={19} q="Какие виды JOIN бывают и чем они отличаются?">
          <code>INNER JOIN</code> — только строки, у которых есть совпадение в обеих таблицах. <code>LEFT
          JOIN</code> — все строки левой таблицы плюс совпадения из правой (если совпадения нет — NULL).{' '}
          <code>RIGHT JOIN</code> — зеркально, все строки правой таблицы. <code>FULL JOIN</code> — все строки из
          обеих таблиц, совпавшие и несовпавшие.
          <TheoryCode language="sql" code={`SELECT u.name, o.id AS order_id
FROM users u
LEFT JOIN orders o ON o.user_id = u.id;
-- вернёт всех пользователей, включая тех, у кого ещё нет заказов (order_id = NULL)`} />
        </QA>

        <QA n={20} q="Что такое оконные функции (window functions)?">
          Функции, которые считают агрегат (сумму, среднее, ранг) для каждой строки, но не схлопывают строки в
          одну, как обычный <code>GROUP BY</code> — строка остаётся, просто рядом с ней добавляется вычисленное
          значение по «окну» связанных строк.
          <TheoryCode language="sql" code={`SELECT
    name,
    department,
    salary,
    RANK() OVER (PARTITION BY department ORDER BY salary DESC) AS rank_in_dept
FROM employees;
-- каждому сотруднику присваивается ранг зарплаты внутри его отдела`} />
        </QA>

        <QA n={21} q="Что такое подзапрос (subquery)?">
          Запрос, вложенный внутрь другого запроса — используется там, где результат одного запроса становится
          входными данными для другого (например, в условии <code>WHERE</code> или как временная таблица во
          <code> FROM</code>).
          <TheoryCode language="sql" code={`SELECT name FROM users
WHERE id IN (
    SELECT user_id FROM orders WHERE amount > 5000
);`} />
        </QA>

        <QA n={22} q="Что такое ETL и зачем аналитику это знать?">
          Extract, Transform, Load — процесс извлечения данных из источников, их преобразования (очистка,
          агрегация, приведение форматов) и загрузки в хранилище, откуда аналитик уже строит отчёты. Понимание ETL
          помогает разобраться, откуда берутся данные в таблицах и почему иногда там встречаются задержки или
          неточности.
        </QA>

        <QA n={23} q="Что такое дашборд и какие метрики в него обычно включают?">
          Визуальная панель с ключевыми показателями, обновляющаяся автоматически, чтобы команда могла быстро
          оценить состояние продукта или бизнеса без ручного построения отчётов. Обычно включает основную
          («северную звезду») метрику, несколько поддерживающих метрик и guardrail-показатели, которые не должны
          ухудшаться.
        </QA>

        <QA n={24} q="Что значит «data-driven» подход к решениям?">
          Принятие решений на основе анализа данных и результатов экспериментов, а не только интуиции или мнения
          самого громкого человека в комнате. Не исключает экспертизу и здравый смысл, но требует подкреплять
          важные решения измеримыми фактами там, где это возможно.
        </QA>
      </section>
    </div>
  )
}
