// Неделя 4, тема «Docker». Только основы, которые реально нужны:
// образ, контейнер, Dockerfile, тома, сети, compose.

export const DOCKER_CHAPTERS = [
  {
    id: 'docker-why',
    title: 'Зачем нужен Docker',
    short: 'Зачем',
    difficulty: 'easy',
    blocks: [
      { t: 'p', v: 'Знаменитая фраза «у меня на компьютере работает» — это ровно та проблема, которую решает Docker.' },

      { t: 'h', v: 'Откуда берётся проблема' },
      { t: 'p', v: 'Приложение — это не только твой код. Это ещё Python нужной версии, полтора десятка библиотек, системные пакеты, переменные окружения, настройки. У тебя Python 3.12 и macOS, у коллеги 3.9 и Windows, на сервере Ubuntu и вообще Python 3.10. Что-то из этого обязательно разойдётся.' },

      { t: 'viz', kind: 'cards', v: [
        { title: 'Без Docker', text: 'Каждый настраивает окружение сам по инструкции. Инструкция устаревает, шаги пропускаются, версии расходятся. Новичок тратит день на запуск проекта.' },
        { title: 'С Docker', text: 'Окружение описано в файле и лежит в репозитории. Команда docker compose up — и всё поднялось одинаково у всех и на сервере.' },
      ] },

      { t: 'h', v: 'Что такое контейнер' },
      { t: 'p', v: '**Контейнер** — изолированный процесс со своей файловой системой, своими библиотеками и своей сетью. Снаружи это обычный процесс операционной системы, изнутри он выглядит как отдельная машина.' },
      { t: 'p', v: 'Контейнер не знает, что происходит на хосте, и не может случайно задеть его файлы или другие контейнеры. Каждое приложение живёт в своей коробке.' },

      { t: 'viz', kind: 'grid', title: 'Контейнер против виртуальной машины',
        head: ['', 'Виртуальная машина', 'Контейнер'],
        rows: [
          ['Что внутри', 'полная гостевая ОС со своим ядром', 'только приложение и его зависимости'],
          ['Размер', 'гигабайты', 'десятки-сотни мегабайт'],
          ['Запуск', 'минута и больше', 'секунда'],
          ['Ядро', 'своё', 'общее с хостом'],
          ['Сколько запустить на ноутбуке', 'две-три', 'десятки'],
        ],
        marks: { '1,2': 'hit', '2,2': 'hit' },
        caption: 'Контейнеры легче, потому что не тащат с собой отдельную операционную систему — они переиспользуют ядро хоста.' },

      { t: 'h', v: 'Что это даёт на практике' },
      { t: 'ul', v: [
        '**Одинаковое окружение** у всех разработчиков и на сервере — расхождениям неоткуда взяться.',
        '**Быстрый старт проекта:** склонировал репозиторий, поднял, работаешь. Без установки Python, PostgreSQL и Redis вручную.',
        '**Изоляция версий:** один проект на Python 3.9, другой на 3.12 — мирно уживаются на одной машине.',
        '**Простой деплой:** образ, проверенный локально, приезжает на сервер тем же самым.',
      ] },

      { t: 'note', v: 'Docker не делает приложение быстрее и не заменяет тесты. Он делает окружение воспроизводимым — и уже это решает больше половины проблем при работе в команде.' },
    ],
    selfCheck: [
      { q: 'Какую проблему решает Docker?', a: 'Расхождение окружений. Приложение зависит от версии языка, библиотек, системных пакетов и настроек, и у каждого разработчика они свои. Docker описывает окружение файлом, и оно воспроизводится одинаково везде.' },
      { q: 'Чем контейнер отличается от виртуальной машины?', a: 'Виртуальная машина содержит полноценную гостевую ОС со своим ядром и весит гигабайты. Контейнер переиспользует ядро хоста и содержит только приложение с зависимостями, поэтому запускается за секунду и весит в десятки раз меньше.' },
      { q: 'Что значит «контейнер изолирован»?', a: 'У него своя файловая система, свои процессы и своя сеть. Он не видит файлы хоста и других контейнеров, если ему это явно не разрешили.' },
    ],
    quiz: {
      question: 'Проект работает у одного разработчика и падает у другого на той же ветке. Что вероятнее всего решит Docker?',
      options: [
        'Ускорит выполнение кода',
        'Даст обоим одинаковое окружение: версии языка, библиотек и системных пакетов',
        'Автоматически исправит ошибки в коде',
        'Заменит тесты',
      ],
      correct: 1,
      explanation: 'Одинаковый код, разное поведение — почти всегда следствие разного окружения. Docker фиксирует его в файле, и у обоих запускается ровно одно и то же.',
    },
  },

  {
    id: 'docker-image',
    title: 'Образ и контейнер',
    short: 'Образ',
    difficulty: 'easy',
    blocks: [
      { t: 'p', v: 'Два самых путаемых понятия в Docker. Разница как между классом и объектом.' },

      { t: 'viz', kind: 'cards', v: [
        { title: 'Образ (image)', text: 'Готовый шаблон: файловая система с приложением и зависимостями. Неизменяемый, лежит на диске или в реестре. Это то, что собирают.' },
        { title: 'Контейнер (container)', text: 'Запущенный экземпляр образа. Их можно поднять хоть десять из одного образа, и каждый будет работать сам по себе. Это то, что выполняется.' },
      ] },

      { t: 'p', v: 'Образ — как установочный диск, контейнер — как установленная и работающая система. Из одного диска можно поставить систему на сколько угодно машин.' },

      { t: 'h', v: 'Первый запуск' },
      { t: 'code', lang: 'bash', v: '# Скачать образ из Docker Hub и запустить контейнер\ndocker run hello-world\n\n# Запустить PostgreSQL\ndocker run -d \\\n  --name mydb \\\n  -e POSTGRES_PASSWORD=secret \\\n  -p 5432:5432 \\\n  postgres:16' },
      { t: 'viz', kind: 'grid', title: 'Флаги docker run, которые нужны почти всегда',
        head: ['Флаг', 'Что делает'],
        rows: [
          ['-d', 'запустить в фоне (detached)'],
          ['--name', 'дать контейнеру имя, иначе будет случайное'],
          ['-p 5432:5432', 'пробросить порт: порт хоста : порт контейнера'],
          ['-e KEY=value', 'передать переменную окружения'],
          ['-v путь:путь', 'подключить том или папку хоста'],
          ['--rm', 'удалить контейнер сразу после остановки'],
        ],
        marks: { '2,0': 'hit' },
        caption: 'Проброс портов запоминается плохо: слева всегда порт хоста, справа — порт внутри контейнера.' },

      { t: 'h', v: 'Команды на каждый день' },
      { t: 'code', lang: 'bash', v: '# Что запущено\ndocker ps\ndocker ps -a              # включая остановленные\n\n# Управление\ndocker stop mydb\ndocker start mydb\ndocker restart mydb\ndocker rm mydb            # удалить контейнер (остановленный)\n\n# Логи\ndocker logs mydb\ndocker logs -f mydb       # следить за новыми строками\n\n# Зайти внутрь контейнера\ndocker exec -it mydb bash\ndocker exec -it mydb psql -U postgres\n\n# Образы\ndocker images             # что скачано\ndocker pull redis:7       # скачать\ndocker rmi redis:7        # удалить образ' },

      { t: 'h', v: 'Теги: версия образа' },
      { t: 'code', lang: 'bash', v: 'postgres:16          # конкретная версия — так и надо\npostgres:16.2        # ещё точнее\npostgres:latest      # последняя на момент скачивания\npostgres             # то же, что latest' },
      { t: 'note', v: 'В рабочих проектах `latest` не используют: сегодня это PostgreSQL 16, через полгода 17, и сборка неожиданно поедет. Всегда фиксируй версию явно.' },

      { t: 'h', v: 'Контейнер эфемерен' },
      { t: 'p', v: 'Ключевая идея, которую важно принять сразу: **всё, что записано внутрь контейнера, исчезает при его удалении**. Создал базу, залил данные, удалил контейнер — данных больше нет.' },
      { t: 'p', v: 'Это не баг, а замысел: контейнеры задуманы одноразовыми, их удаляют и пересоздают свободно. Данные, которые должны пережить контейнер, выносят в тома — о них отдельная глава.' },
    ],
    selfCheck: [
      { q: 'В чём разница между образом и контейнером?', a: 'Образ — неизменяемый шаблон с приложением и зависимостями, его собирают. Контейнер — запущенный экземпляр образа, и из одного образа можно запустить сколько угодно контейнеров.' },
      { q: 'Что означает -p 8080:80?', a: 'Проброс порта: обращение к порту 8080 на хосте попадёт на порт 80 внутри контейнера. Слева всегда хост, справа контейнер.' },
      { q: 'Почему в рабочих проектах не используют тег latest?', a: 'Он означает «последняя версия на момент скачивания». Через полгода это будет другая версия, и сборка, которая работала, неожиданно сломается. Версию фиксируют явно.' },
      { q: 'Что произойдёт с данными, записанными внутрь контейнера, после его удаления?', a: 'Они исчезнут. Файловая система контейнера живёт ровно столько, сколько сам контейнер. Данные, которые должны сохраниться, выносят в тома.' },
    ],
    quiz: {
      question: 'Запустили PostgreSQL через docker run, залили данные, сделали docker rm. Что с данными?',
      options: [
        'Сохранятся, Docker хранит их отдельно',
        'Удалены вместе с контейнером',
        'Перенесутся в образ',
        'Сохранятся, если контейнер был запущен с флагом -d',
      ],
      correct: 1,
      explanation: 'Файловая система контейнера существует только пока существует сам контейнер. Чтобы данные пережили пересоздание, их нужно было положить в том через флаг -v.',
    },
  },

  {
    id: 'docker-dockerfile',
    title: 'Dockerfile: собираем свой образ',
    short: 'Dockerfile',
    difficulty: 'medium',
    blocks: [
      { t: 'p', v: 'Готовые образы хороши для PostgreSQL и Redis. Своё приложение нужно упаковать самому — для этого пишут **Dockerfile**, пошаговую инструкцию сборки.' },

      { t: 'code', lang: 'dockerfile', v: '# Базовый образ, от которого отталкиваемся\nFROM python:3.12-slim\n\n# Рабочая папка внутри контейнера\nWORKDIR /app\n\n# Сначала только зависимости — про это ниже\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Теперь код приложения\nCOPY . .\n\n# Порт, который слушает приложение (документирующая строка)\nEXPOSE 8000\n\n# Команда запуска контейнера\nCMD ["python", "main.py"]' },

      { t: 'viz', kind: 'grid', title: 'Основные инструкции',
        head: ['Инструкция', 'Что делает'],
        rows: [
          ['FROM', 'базовый образ — с чего начинаем'],
          ['WORKDIR', 'рабочая папка для последующих команд'],
          ['COPY', 'скопировать файлы с хоста в образ'],
          ['RUN', 'выполнить команду при сборке'],
          ['ENV', 'переменная окружения'],
          ['EXPOSE', 'объявить порт (только документация)'],
          ['CMD', 'команда при запуске контейнера'],
        ],
        caption: 'RUN выполняется один раз при сборке образа, CMD — каждый раз при запуске контейнера. Их постоянно путают.' },

      { t: 'h', v: 'Сборка и запуск' },
      { t: 'code', lang: 'bash', v: '# Собрать образ из Dockerfile в текущей папке\ndocker build -t myapp:1.0 .\n\n# Запустить\ndocker run -d -p 8000:8000 --name myapp myapp:1.0' },
      { t: 'p', v: 'Точка в конце `build` — это **контекст сборки**, папка, из которой Docker берёт файлы для `COPY`. Забыть её — самая частая ошибка новичка.' },

      { t: 'h', v: 'Слои и кеш: почему порядок строк важен' },
      { t: 'p', v: 'Каждая инструкция создаёт **слой**. При повторной сборке Docker переиспользует слои, которые не изменились, и начинает пересобирать с первого изменившегося.' },

      { t: 'viz', kind: 'steps', title: 'Две версии одного Dockerfile',
        frames: [
          { label: 'Плохо: сначала весь код', cells: ['COPY . .', 'RUN pip install'], marks: { 0: 'miss', 1: 'miss' },
            note: 'Поменял одну строку в коде — слой COPY изменился, и pip install запускается заново. Каждая сборка по несколько минут.' },
          { label: 'Хорошо: сначала зависимости', cells: ['COPY requirements.txt', 'RUN pip install', 'COPY . .'], marks: { 0: 'hit', 1: 'hit', 2: 'look' },
            note: 'Код поменялся — пересобирается только последний слой. Зависимости берутся из кеша, сборка занимает секунды.' },
        ],
        caption: 'Правило: то, что меняется редко, ставь выше; то, что меняется часто, — ниже. Зависимости меняются реже кода.' },

      { t: 'h', v: 'Файл .dockerignore' },
      { t: 'p', v: 'Работает как `.gitignore`: перечисленное в нём не попадёт в контекст сборки. Без него в образ уедут `.git`, `venv` и `node_modules` — сотни мегабайт мусора.' },
      { t: 'code', v: '.git\n__pycache__\n*.pyc\nvenv/\nnode_modules/\n.env\n*.log' },
      { t: 'note', v: 'Отдельно обрати внимание на `.env` в списке. Секреты не должны попадать в образ: образ можно распаковать и прочитать. Переменные передают при запуске через `-e` или `env_file`.' },

      { t: 'h', v: 'Выбор базового образа' },
      { t: 'viz', kind: 'grid', title: 'Что брать за основу',
        head: ['Образ', 'Размер', 'Когда'],
        rows: [
          ['python:3.12', 'около 1 ГБ', 'когда нужен полный набор системных пакетов'],
          ['python:3.12-slim', 'около 150 МБ', 'обычный выбор для большинства приложений'],
          ['python:3.12-alpine', 'около 50 МБ', 'минимум размера, но бывают проблемы со сборкой пакетов'],
        ],
        marks: { '1,0': 'hit' },
        caption: 'По умолчанию бери slim. Alpine меньше, но использует другую стандартную библиотеку C, и часть Python-пакетов приходится собирать из исходников.' },
    ],
    selfCheck: [
      { q: 'Чем RUN отличается от CMD?', a: 'RUN выполняется один раз во время сборки образа и его результат сохраняется в слое. CMD задаёт команду, которая запускается каждый раз при старте контейнера.' },
      { q: 'Почему requirements.txt копируют отдельно и до остального кода?', a: 'Чтобы слой с установкой зависимостей попадал в кеш. Код меняется часто, зависимости редко — при таком порядке правка кода не заставляет переустанавливать все пакеты заново.' },
      { q: 'Зачем нужен .dockerignore?', a: 'Чтобы исключить из контекста сборки лишнее: папку .git, виртуальные окружения, node_modules и файлы с секретами. Иначе они попадут в образ, раздув его и создав риск утечки.' },
      { q: 'Что означает точка в конце docker build -t myapp .?', a: 'Это контекст сборки — папка, из которой Docker берёт файлы для инструкций COPY. Чаще всего текущая, но забыть её нельзя, иначе команда не выполнится.' },
    ],
    quiz: {
      question: 'Почему в Dockerfile сначала копируют requirements.txt, а только потом весь код?',
      options: [
        'Иначе pip не найдёт файл',
        'Чтобы слой с установкой зависимостей не пересобирался при каждом изменении кода',
        'Так требует синтаксис Dockerfile',
        'Чтобы образ получился меньше',
      ],
      correct: 1,
      explanation: 'Docker кеширует слои и пересобирает всё начиная с первого изменившегося. Если код скопировать до установки зависимостей, любая правка кода будет заставлять переустанавливать все пакеты заново.',
    },
  },

  {
    id: 'docker-volumes',
    title: 'Тома, сети и переменные окружения',
    short: 'Тома и сети',
    difficulty: 'medium',
    blocks: [
      { t: 'p', v: 'Три вещи, без которых контейнер остаётся игрушкой: где хранить данные, как контейнерам общаться и как передавать настройки.' },

      { t: 'h', v: 'Тома: данные переживают контейнер' },
      { t: 'p', v: 'Мы уже знаем, что файловая система контейнера исчезает вместе с ним. **Том (volume)** — хранилище, которое живёт отдельно и подключается к контейнеру.' },
      { t: 'code', lang: 'bash', v: '# Именованный том: Docker хранит его у себя\ndocker volume create pgdata\ndocker run -d --name mydb \\\n  -v pgdata:/var/lib/postgresql/data \\\n  -e POSTGRES_PASSWORD=secret \\\n  postgres:16\n\n# Теперь контейнер можно удалить и создать заново — данные на месте\ndocker rm -f mydb\ndocker run -d --name mydb -v pgdata:/var/lib/postgresql/data ... postgres:16' },

      { t: 'viz', kind: 'grid', title: 'Два способа подключить хранилище',
        head: ['Способ', 'Синтаксис', 'Когда'],
        rows: [
          ['Именованный том', '-v pgdata:/path', 'данные приложения: базы, загруженные файлы'],
          ['Папка хоста (bind mount)', '-v ./src:/app/src', 'разработка: код виден контейнеру сразу'],
        ],
        marks: { '0,0': 'hit' },
        caption: 'Для данных — именованный том, Docker управляет им сам. Для кода при разработке — папка хоста, чтобы правки подхватывались без пересборки.' },

      { t: 'h', v: 'Сети: как контейнеры находят друг друга' },
      { t: 'p', v: 'Контейнеры в одной сети видят друг друга **по имени** — Docker поднимает для этого внутренний DNS.' },
      { t: 'code', lang: 'bash', v: 'docker network create mynet\n\ndocker run -d --name db      --network mynet postgres:16\ndocker run -d --name backend --network mynet myapp:1.0\n\n# Внутри backend адрес базы — просто "db"\n# postgresql://user:pass@db:5432/shop' },
      { t: 'note', v: 'Частая ошибка: указать в настройках бэкенда `localhost:5432`. Внутри контейнера `localhost` — это он сам, а не хост и не соседний контейнер. Адресом служит имя контейнера.' },

      { t: 'h', v: 'Порты: наружу и внутрь' },
      { t: 'p', v: 'Важно различать две вещи. Внутри общей сети контейнеры общаются по внутренним портам напрямую — пробрасывать ничего не нужно. Флаг `-p` нужен только чтобы достучаться до контейнера **снаружи**, с хоста.' },
      { t: 'code', lang: 'bash', v: '# Бэкенд доступен из браузера\ndocker run -d -p 8000:8000 --network mynet backend\n\n# Базе проброс наружу не нужен и даже вреден:\n# к ней ходит только бэкенд изнутри сети\ndocker run -d --network mynet postgres:16' },

      { t: 'h', v: 'Переменные окружения' },
      { t: 'code', lang: 'bash', v: '# По одной\ndocker run -e DATABASE_URL=postgresql://... -e DEBUG=false myapp\n\n# Файлом\ndocker run --env-file .env myapp' },
      { t: 'p', v: 'Через переменные передают всё, что различается между окружениями: адрес базы, ключи, режим отладки. Один и тот же образ работает и локально, и на сервере — меняются только переменные.' },
      { t: 'note', v: 'Именно поэтому секреты не пишут в Dockerfile. Образ неизменяем и его можно распаковать: зашитый пароль увидит любой, кто получит образ.' },
    ],
    selfCheck: [
      { q: 'Зачем нужен том?', a: 'Чтобы данные пережили удаление контейнера. Файловая система контейнера исчезает вместе с ним, а том хранится отдельно и подключается к новому контейнеру как есть.' },
      { q: 'Как один контейнер обращается к другому?', a: 'По имени контейнера, если оба в одной сети Docker — внутренний DNS разрешит имя в нужный адрес. Указывать localhost нельзя: внутри контейнера это он сам.' },
      { q: 'Нужно ли пробрасывать порт базы через -p, если к ней ходит только бэкенд?', a: 'Нет. Внутри общей сети контейнеры общаются напрямую по внутренним портам. Проброс открыл бы базу наружу без необходимости, что ещё и небезопасно.' },
      { q: 'Почему секреты не пишут в Dockerfile?', a: 'Образ неизменяем и его можно распаковать и прочитать, поэтому зашитый пароль увидит любой, кто получит образ. Секреты передают при запуске через переменные окружения.' },
    ],
    quiz: {
      question: 'Бэкенд в контейнере не может подключиться к базе, в настройках указано localhost:5432. В чём дело?',
      options: [
        'Нужно пробросить порт базы через -p',
        'Внутри контейнера localhost — это он сам; адресом должно быть имя контейнера базы',
        'PostgreSQL не работает в Docker',
        'Не хватает флага -d',
      ],
      correct: 1,
      explanation: 'Каждый контейнер имеет собственный сетевой стек, и localhost внутри него указывает на него самого. Соседний контейнер в той же сети доступен по своему имени — например, db:5432.',
    },
  },

  {
    id: 'docker-compose',
    title: 'docker compose: несколько контейнеров вместе',
    short: 'compose',
    difficulty: 'medium',
    blocks: [
      { t: 'p', v: 'Настоящее приложение — это не один контейнер. Бэкенд, база, кеш, иногда фронтенд. Запускать их по очереди командами `docker run` с десятком флагов невозможно. Для этого есть **compose**.' },

      { t: 'p', v: 'Всё описывается одним файлом `docker-compose.yml`, и весь набор поднимается одной командой.' },

      { t: 'code', lang: 'yaml', v: 'services:\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: app\n      POSTGRES_PASSWORD: secret\n      POSTGRES_DB: shop\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U app"]\n      interval: 5s\n      retries: 5\n\n  redis:\n    image: redis:7\n\n  backend:\n    build: .\n    ports:\n      - "8000:8000"\n    environment:\n      DATABASE_URL: postgresql://app:secret@db:5432/shop\n      REDIS_URL: redis://redis:6379\n    depends_on:\n      db:\n        condition: service_healthy\n    volumes:\n      - ./src:/app/src\n\nvolumes:\n  pgdata:' },

      { t: 'viz', kind: 'grid', title: 'Что означает каждый ключ',
        head: ['Ключ', 'Что делает'],
        rows: [
          ['image', 'взять готовый образ из реестра'],
          ['build', 'собрать образ из Dockerfile в указанной папке'],
          ['ports', 'проброс наружу, как флаг -p'],
          ['environment', 'переменные окружения'],
          ['volumes', 'тома и папки хоста'],
          ['depends_on', 'порядок запуска'],
          ['healthcheck', 'как понять, что сервис готов принимать запросы'],
        ],
        caption: 'Compose сам создаёт общую сеть, поэтому сервисы видят друг друга по именам — db, redis, backend.' },

      { t: 'h', v: 'Команды' },
      { t: 'code', lang: 'bash', v: 'docker compose up -d          # поднять всё в фоне\ndocker compose up --build    # пересобрать образы и поднять\ndocker compose ps            # что работает\ndocker compose logs -f backend  # логи одного сервиса\ndocker compose exec backend bash # зайти внутрь\ndocker compose restart backend\ndocker compose down          # остановить и удалить контейнеры\ndocker compose down -v       # ...вместе с томами — данные пропадут' },
      { t: 'note', v: '`docker compose down -v` удаляет тома, а значит и базу целиком. Флаг `-v` здесь очень легко добавить по привычке и потерять локальные данные.' },

      { t: 'h', v: 'depends_on и готовность сервиса' },
      { t: 'p', v: 'Тонкий момент, на котором спотыкаются все. Обычный `depends_on` гарантирует только **порядок запуска**, а не готовность: контейнер базы уже стартовал, но PostgreSQL внутри него ещё инициализируется секунд десять. Бэкенд в это время падает с ошибкой подключения.' },
      { t: 'p', v: 'Решение — `healthcheck` плюс `condition: service_healthy`, как в примере выше. Тогда compose дождётся, пока база действительно начнёт отвечать.' },

      { t: 'h', v: 'Типичный рабочий процесс' },
      { t: 'code', lang: 'bash', v: 'git clone https://github.com/team/project.git\ncd project\ncp .env.example .env         # заполнить свои значения\ndocker compose up -d --build\n\n# приложение доступно на localhost:8000\n# база поднята, миграции применены, можно работать' },
      { t: 'p', v: 'Ради этих четырёх команд Docker и нужен. Без него новый человек в команде тратил бы день на установку и настройку всего по списку.' },
    ],
    selfCheck: [
      { q: 'Что делает docker compose up?', a: 'Поднимает все сервисы, описанные в docker-compose.yml: создаёт общую сеть, при необходимости собирает образы, запускает контейнеры и подключает тома.' },
      { q: 'Как сервисы в compose находят друг друга?', a: 'По имени сервиса из файла — compose создаёт общую сеть с внутренним DNS. Бэкенд обращается к базе по адресу db:5432.' },
      { q: 'Почему depends_on не гарантирует, что база готова?', a: 'Он обеспечивает только порядок запуска контейнеров. Сам PostgreSQL внутри контейнера инициализируется ещё несколько секунд, и всё это время подключения отвергаются. Нужен healthcheck с условием service_healthy.' },
      { q: 'Чем опасна команда docker compose down -v?', a: 'Флаг -v удаляет тома вместе с контейнерами, то есть стирает данные базы. Без него тома сохраняются и при следующем подъёме данные остаются на месте.' },
    ],
    quiz: {
      question: 'Бэкенд в compose падает при старте с ошибкой подключения к базе, хотя depends_on указан. Почему?',
      options: [
        'depends_on не работает в новых версиях compose',
        'depends_on ждёт только запуска контейнера, а не готовности PostgreSQL принимать подключения',
        'Нужно пробросить порт базы наружу',
        'База и бэкенд в разных сетях',
      ],
      correct: 1,
      explanation: 'Контейнер стартует мгновенно, а PostgreSQL внутри него инициализируется ещё несколько секунд. Чтобы compose дождался реальной готовности, нужен healthcheck и depends_on с condition: service_healthy.',
    },
  },
]
