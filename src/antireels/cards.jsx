import { Card, P, B, Formula, Code, Ul, Fig } from './CardKit'

// ─────────────────────────────────────────────────────────────
// Хранилище карточек AntiReels. Каждая карточка — адаптивный div
// (компонент Card). Темы взяты из программы июня и 1 июля.
// Добавляй новые карточки в массив CARDS ниже.
// ─────────────────────────────────────────────────────────────

const cards = []
const mk = (track, title, body) => { cards.push({ id: cards.length, track, title, body }) }

/* ═══════════════ FRONTEND ═══════════════ */

mk('frontend', 'Скелет HTML-страницы', (
  <>
    <P>Любая веб-страница начинается с одинакового каркаса. <B>HTML</B> описывает не логику, а структуру: где заголовок, где абзац, где картинка.</P>
    <Code>{`<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8">
    <title>Моя страница</title>
  </head>
  <body>
    <h1>Привет!</h1>
  </body>
</html>`}</Code>
    <P><B>head</B> — служебные данные (кодировка, заголовок вкладки), <B>body</B> — всё видимое содержимое.</P>
  </>
))

mk('frontend', 'Семантические теги', (
  <>
    <P>Семантика — использование тегов <B>по смыслу</B>, а не сплошных <code>&lt;div&gt;</code>. Такие теги понятны браузеру, поисковикам и скринридерам.</P>
    <Ul items={[
      '<header> — шапка, <nav> — навигация',
      '<main> — основное содержимое',
      '<section>, <article> — смысловые блоки',
      '<footer> — подвал',
    ]} />
    <P>Правильная семантика улучшает <B>доступность</B> и <B>SEO</B> — это то, что отличает джуна от новичка.</P>
  </>
))

mk('frontend', 'Формы и типы input', (
  <>
    <P>Формы собирают данные пользователя. Тип поля задаёт поведение и встроенную валидацию.</P>
    <Code>{`<label for="email">Почта</label>
<input type="email" id="email" required>`}</Code>
    <Ul items={[
      'text — обычный текст',
      'email — почта с проверкой формата',
      'tel — телефон, password — скрытый ввод',
      'checkbox / radio — выбор',
    ]} />
    <P>Атрибут <B>for</B> у label = <B>id</B> поля: клик по подписи ставит фокус в поле.</P>
  </>
))

mk('frontend', 'Таблицы в HTML', (
  <>
    <P>Таблица строится из строк и ячеек. Заголовки выделяются отдельным тегом.</P>
    <Code>{`<table>
  <thead>
    <tr><th>Технология</th><th>Уровень</th></tr>
  </thead>
  <tbody>
    <tr><td>HTML</td><td>8/10</td></tr>
  </tbody>
</table>`}</Code>
    <Ul items={[
      '<tr> — строка (row)',
      '<th> — ячейка-заголовок (жирная)',
      '<td> — обычная ячейка (data)',
    ]} />
  </>
))

mk('frontend', 'Валидная вёрстка', (
  <>
    <P>Валидный HTML соответствует стандартам W3C. Проверить можно на <B>validator.w3.org</B>.</P>
    <Ul items={[
      'Каждый парный тег закрыт',
      'Теги вложены без пересечений',
      'Один <h1> на страницу, заголовки по иерархии',
      'У картинок заполнен alt',
      'id уникальны, атрибуты в кавычках',
    ]} />
    <P>Чистая семантичная вёрстка = меньше багов и выше доступность.</P>
  </>
))

mk('frontend', 'Ссылки, картинки и alt', (
  <>
    <P>Ссылки связывают страницы, а картинки оживляют контент. Но у изображения обязателен <B>alt</B>.</P>
    <Code>{`<a href="https://ya.ru">Ссылка</a>
<img src="cat.jpg" alt="Рыжий кот на окне">`}</Code>
    <P>Атрибут <B>alt</B> читают скринридеры для незрячих и он показывается, если картинка не загрузилась. Без него вёрстка невалидна по правилам доступности.</P>
  </>
))

/* ═══════════════ BACKEND ═══════════════ */

mk('backend', 'Python vs Go: типизация', (
  <>
    <P><B>Python</B> — динамическая типизация: тип определяется во время выполнения. Пишется быстро, но ошибки типов всплывают только при запуске.</P>
    <P><B>Go</B> — статическая типизация: типы проверяет компилятор заранее. Больше кода, зато ошибки ловятся до запуска, а бинарник работает очень быстро.</P>
    <Ul items={[
      'Python: интерпретируемый, гибкий, огромная экосистема',
      'Go: компилируемый, быстрый, для высоких нагрузок',
    ]} />
  </>
))

mk('backend', 'Горутины в Go', (
  <>
    <P>Ключевое преимущество Go — <B>горутины</B>: сверхлёгкие «потоки». Их можно запустить тысячами, что делает Go идеальным для серверов с множеством одновременных запросов.</P>
    <Code>{`go doWork()          // запуск параллельно
ch := make(chan int) // канал для обмена`}</Code>
    <P>В Python настоящей многопоточности мешает <B>GIL</B>, но для сетевых задач помогает асинхронность (<code>async/await</code>).</P>
  </>
))

mk('backend', 'Фреймворки Python', (
  <>
    <P>Три популярных выбора для серверной разработки на Python:</P>
    <Ul items={[
      'Django — full-stack «всё из коробки»: ORM, админка, авторизация',
      'Flask — микрофреймворк, минимализм и гибкость',
      'FastAPI — быстрый async, авто-документация OpenAPI',
    ]} />
    <P>Для REST API сегодня чаще всего берут <B>FastAPI</B>, для крупного продукта — <B>Django</B>.</P>
  </>
))

mk('backend', 'Фреймворки Go', (
  <>
    <P>Для веба на Go часто хватает стандартной библиотеки, но есть и удобные фреймворки:</P>
    <Ul items={[
      'net/http — встроен в Go, без зависимостей',
      'Gin — самый популярный, быстрый роутинг и middleware',
      'Echo — минималистичный и производительный',
      'Fiber — вдохновлён Express.js',
    ]} />
    <P>Де-факто стандарт — <B>Gin</B>.</P>
  </>
))

mk('backend', 'REST API: методы и коды', (
  <>
    <P><B>REST</B> — стиль построения API вокруг ресурсов. Действие задаёт HTTP-метод, результат — код ответа.</P>
    <Ul items={[
      'GET — получить, POST — создать',
      'PUT/PATCH — обновить, DELETE — удалить',
      '2xx — успех, 4xx — ошибка клиента, 5xx — сервера',
    ]} />
    <P>Например, <B>200</B> OK, <B>404</B> Not Found, <B>500</B> Internal Server Error.</P>
  </>
))

mk('backend', 'Базы данных и JOIN', (
  <>
    <P>Реляционная БД хранит данные в таблицах, связанных ключами. <B>JOIN</B> соединяет строки из разных таблиц.</P>
    <Code>{`SELECT u.name, o.total
FROM users u
JOIN orders o ON o.user_id = u.id;`}</Code>
    <Ul items={[
      'INNER JOIN — только совпадающие строки',
      'LEFT JOIN — все слева + совпадения справа',
    ]} />
  </>
))

/* ═══════════════ АНАЛИТИКА ═══════════════ */

mk('analytics', 'Типы данных в Python', (
  <>
    <P>Аналитика начинается с уверенного владения базовыми типами Python.</P>
    <Ul items={[
      'int, float — числа',
      'str — строки, bool — True/False',
      'list — изменяемый список, dict — ключ→значение',
      'set — уникальные значения',
    ]} />
    <Code>{`x = 42
name = "Аня"
prices = [100, 250, 90]`}</Code>
  </>
))

mk('analytics', 'Коллекции: list, dict, set', (
  <>
    <P>Три рабочие лошадки анализа данных:</P>
    <Code>{`nums = [3, 1, 2]        # список
user = {"age": 25}      # словарь
unique = set([1,1,2])   # {1, 2}`}</Code>
    <Ul items={[
      'list — упорядочен, есть дубликаты',
      'dict — быстрый доступ по ключу',
      'set — только уникальные, быстрая проверка вхождения',
    ]} />
  </>
))

mk('analytics', 'SQL: SELECT и GROUP BY', (
  <>
    <P>SQL — язык запросов к базам данных. Основа аналитики — выборка и агрегация.</P>
    <Code>{`SELECT city, COUNT(*)
FROM users
WHERE age > 18
GROUP BY city;`}</Code>
    <P><B>WHERE</B> фильтрует строки, <B>GROUP BY</B> группирует, а агрегаты (<code>COUNT, SUM, AVG</code>) считают внутри групп.</P>
  </>
))

mk('analytics', 'pandas: DataFrame', (
  <>
    <P><B>pandas</B> — главная библиотека для таблиц в Python. Структура <B>DataFrame</B> — это таблица со строками и колонками.</P>
    <Code>{`import pandas as pd
df = pd.DataFrame({
  "город": ["Мск", "Спб"],
  "население": [13, 5]
})
df["население"].sum()  # 18`}</Code>
    <P>Фильтрация, группировка, объединение — всё в пару строк.</P>
  </>
))

mk('analytics', 'Big O: оценка сложности', (
  <>
    <P><B>Big O</B> показывает, как растёт время работы алгоритма с ростом данных. Важно для обработки больших датасетов.</P>
    <Formula>O(1) &lt; O(log n) &lt; O(n) &lt; O(n log n) &lt; O(n²)</Formula>
    <Ul items={[
      'O(1) — доступ по индексу',
      'O(n) — проход по списку',
      'O(n²) — вложенные циклы (медленно на больших данных)',
    ]} />
  </>
))

mk('analytics', 'Визуализация: matplotlib', (
  <>
    <P><B>matplotlib</B> — базовая библиотека для графиков. Хороший график объясняет данные лучше таблицы цифр.</P>
    <Code>{`import matplotlib.pyplot as plt
plt.plot([1,2,3], [10,20,15])
plt.show()`}</Code>
    <P>Поверх неё работает <B>seaborn</B> — красивые статистические графики почти без настройки.</P>
  </>
))

/* ═══════════════ MACHINE LEARNING ═══════════════ */

mk('ml', 'Что такое Machine Learning', (
  <>
    <P><B>Машинное обучение</B> — это подход, где модель сама находит закономерности в данных, а не следует жёстко прописанным правилам.</P>
    <Ul items={[
      'Обучение с учителем — есть правильные ответы (метки)',
      'Без учителя — ищем структуру (кластеры)',
      'С подкреплением — обучение через награды',
    ]} />
    <P>Данные + признаки + алгоритм → модель, которая делает предсказания.</P>
  </>
))

mk('ml', 'NumPy: быстрые массивы', (
  <>
    <P><B>NumPy</B> — фундамент вычислений в Python. Массивы работают в разы быстрее списков за счёт векторизации.</P>
    <Code>{`import numpy as np
a = np.array([1, 2, 3])
a * 2        # [2 4 6]
a.mean()     # 2.0`}</Code>
    <P>На NumPy построены pandas, scikit-learn и почти вся ML-экосистема.</P>
  </>
))

mk('ml', 'Линейная регрессия', (
  <>
    <P>Простейшая модель: предсказываем число как линейную функцию признаков.</P>
    <Formula>ŷ = w·x + b</Formula>
    <P>Модель подбирает веса <B>w</B> и сдвиг <B>b</B>, минимизируя ошибку. Метрика ошибки — средний квадрат отклонений:</P>
    <Formula>MSE = (1/n) · Σ (yᵢ − ŷᵢ)²</Formula>
  </>
))

mk('ml', 'scikit-learn', (
  <>
    <P><B>scikit-learn</B> — библиотека классических алгоритмов ML с единым удобным интерфейсом.</P>
    <Code>{`from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(X_train, y_train)
model.predict(X_test)`}</Code>
    <P>Один и тот же паттерн <B>fit → predict</B> работает для регрессии, классификации и кластеризации.</P>
  </>
))

mk('ml', 'Обучающая и тестовая выборки', (
  <>
    <P>Чтобы честно оценить модель, данные делят на части: на одной учат, на другой проверяют.</P>
    <Ul items={[
      'Train — модель учится на этих данных',
      'Test — проверяем на невиданных примерах',
      'Переобучение (overfitting) — модель «зазубрила» train и плохо работает на test',
    ]} />
    <P>Хорошая модель <B>обобщает</B>, а не запоминает.</P>
  </>
))

mk('ml', 'Признаки (features)', (
  <>
    <P><B>Признак</B> — измеримое свойство объекта, которое подаётся модели. Качество признаков часто важнее выбора алгоритма.</P>
    <Ul items={[
      'Числовые: возраст, цена, площадь',
      'Категориальные: город, тип (кодируются числами)',
      'Feature engineering — создание новых полезных признаков',
    ]} />
    <P>Мусор на входе → мусор на выходе. Данные решают.</P>
  </>
))

/* ═══════════════ КИБЕРБЕЗОПАСНОСТЬ ═══════════════ */

mk('security', 'Триада CIA', (
  <>
    <P>Вся информационная безопасность строится вокруг трёх свойств информации:</P>
    <Ul items={[
      'Confidentiality — доступ только у тех, кому положено',
      'Integrity — данные не изменены незаметно',
      'Availability — система доступна, когда нужна',
    ]} />
    <Fig caption="C — I — A: три кита защиты">
      <svg viewBox="0 0 220 120" width="200" height="110" xmlns="http://www.w3.org/2000/svg">
        <polygon points="110,12 205,105 15,105" fill="none" stroke="#f87171" strokeWidth="2" />
        <text x="110" y="40" fill="#f87171" fontSize="13" fontWeight="700" textAnchor="middle">C</text>
        <text x="55" y="98" fill="#f87171" fontSize="13" fontWeight="700" textAnchor="middle">I</text>
        <text x="165" y="98" fill="#f87171" fontSize="13" fontWeight="700" textAnchor="middle">A</text>
      </svg>
    </Fig>
  </>
))

mk('security', 'Аутентификация ≠ авторизация', (
  <>
    <P>Два похожих слова с разным смыслом. Сначала одно, потом другое.</P>
    <Ul items={[
      'Аутентификация — «ты действительно это ты?» (пароль, 2FA)',
      'Авторизация — «что тебе можно?» (права доступа)',
    ]} />
    <P>Сначала система <B>подтверждает личность</B>, затем <B>проверяет права</B>. Путать их — классическая ошибка.</P>
  </>
))

mk('security', 'Частые атаки', (
  <>
    <P>Топ угроз, о которых должен знать каждый разработчик:</P>
    <Ul items={[
      'SQL-инъекция — внедрение SQL через поля ввода',
      'XSS — вредоносный скрипт на странице',
      'Фишинг — обман, чтобы выманить данные',
      'DDoS — перегрузка сервиса запросами',
    ]} />
    <P>Большинство закрывается <B>валидацией ввода</B> и разумной архитектурой.</P>
  </>
))

mk('security', 'Хеширование паролей', (
  <>
    <P>Пароли <B>никогда</B> не хранят в открытом виде. Их хранят как необратимый хеш с солью.</P>
    <Formula>hash(пароль + соль) → 9f2a…c1</Formula>
    <Ul items={[
      'bcrypt / Argon2 — правильный выбор',
      'MD5 / SHA1 — устарели, небезопасны',
      'Соль защищает от радужных таблиц',
    ]} />
  </>
))

mk('security', 'Направления в ИБ', (
  <>
    <P>Кибербезопасность — огромная сфера. Основные направления, между которыми можно выбирать:</P>
    <Ul items={[
      'AppSec — безопасность кода и приложений',
      'Pentest / Red Team — легальный взлом',
      'Blue Team / SOC — мониторинг и защита',
      'Криптография, Forensics, GRC, Cloud, DevSecOps',
    ]} />
    <P><B>Red</B> атакует, <B>Blue</B> защищает, <B>Purple</B> — когда они работают вместе.</P>
  </>
))

mk('security', 'Принципы защиты', (
  <>
    <P>Несколько базовых правил, на которых держится безопасность систем:</P>
    <Ul items={[
      'Least Privilege — минимум необходимых прав',
      'Defense in Depth — несколько рубежей защиты',
      'Шифрование данных и регулярные обновления',
      'Резервное копирование — спасает от шифровальщиков',
    ]} />
    <P>Джуну стоит начать с <B>OWASP Top 10</B> — топа веб-уязвимостей.</P>
  </>
))

export const CARDS = cards
export default CARDS
